# GUI UI/UX 审查与修复记录

## 审查范围

- 主工作台：工具入口、搜索、工具卡片、空状态、窗口尺寸与滚动区域。
- 子工具窗口：FA List、Excel 批量合并、TS 管理、TS2、看账工具、函证进度、目录清单等主要入口。
- 通用体验：字体、颜色、按钮规格、表格行高、弹窗居中、屏幕适配、状态提示、动态页面切换后的样式继承。
- 本轮只处理界面布局与样式层面的低成本优化，不调整审计计算、文件读取、文件写入等业务逻辑。

## 主要发现

1. 多个工具窗口历史上直接将控件挂到根窗口，缺少统一的顶部、内容区、底部操作区，导致工具之间视觉割裂。
   - 建议：以 Hub 为基准抽象标准窗口骨架，主要工具统一使用 shell、header、body、footer。

2. 按钮、输入区、状态栏尺寸不一致，部分工具使用固定宽高或局部硬编码，窗口缩放后容易拥挤。
   - 建议：集中管理按钮宽度、页面边距、区块间距和表格行高，并在工具入口处递归规范旧控件尺寸。

3. 旧 tkinter 控件混用不同背景色、提示色和字体，动态创建页面无法自动继承全局主题。
   - 建议：在工具启动和页面切换后执行主题规范化，保留业务提示颜色语义。

4. 部分窗口和弹窗使用固定尺寸，未充分考虑小屏幕或多显示器环境。
   - 建议：统一使用屏幕适配和居中工具，设置合理最小尺寸，避免窗口超出可视区域。

5. 表格区域滚动和伸缩策略分散在各工具内，部分页面主体内容与底部按钮容易挤在一起。
   - 建议：表格和长内容放入可扩展 body，底部操作固定到 footer，滚动条与表格视口绑定。

6. 局部步骤状态仍使用蓝、绿、灰和 Arial 字体硬编码，和全局主题不完全一致。
   - 已处理：FA List 步骤条改为使用统一主题色和中文友好的字体族。

## 已完成修复

- 新增 `PRODUCT.md`：明确本项目是产品型桌面工具界面，默认优化原则为业务优先、低成本、防错、层级清晰、空间高效。
- 新增 `launcher/ui_theme.py`：统一主题、色彩、字体、间距、表格、按钮、窗口适配、旧控件规范化工具。
- 更新 Hub：颜色与窗口主题改用统一 token，作为全局视觉基准。
- 更新工具入口：FA List、Excel Merger、TS、目录清单、看账等入口统一应用主题和控件规范化。
- 重构主要窗口布局：FA List、Excel Merger、TS 管理、TS2、看账工具接入标准 header/body/footer 布局。
- 修复动态页面样式继承：FA List 的文件配置、匹配、预览、列选择、透视、导出等页面切换后自动规范化。
- 统一弹窗体验：表格选择、折旧测算、函证进度、目录清单等窗口补齐主题、居中和屏幕适配。
- 优化 FA List 步骤条：当前步骤、已完成步骤、未完成步骤改用主题色，避免局部硬编码破坏层级一致性。
- 补齐 Excel 批量合并防重复提交：点击“开始合并”后禁用主按钮，状态栏提示处理中；完成、降级完成或失败后恢复按钮并更新状态。
- 补齐 FA List 合并防重复提交：合并线程运行期间重复触发“执行合并”会被拦截，并通过状态栏提示等待当前任务完成；合并进度窗禁止手动关闭。
- 优化看账工具主流程文案：第一屏核心按钮、反馈按钮、说明区和科目筛选弹窗去除装饰性 emoji，改为稳定纯文本，保持内部工具的克制风格。
- 优化 FA List 导出列选择：已选列数量改为实时更新并放入底部操作行；未选择任何列时禁用“下一步”，减少无效点击和底部空间占用。
- 补齐 FA List 导出防重复提交：导出线程运行期间再次触发导出会被拦截，并通过状态栏提示等待当前任务完成。
- 优化 FA List 文件配置校验文案：缺文件、缺工作表、未加载、缺匹配列等阻断提示统一改为“无法进入下一步”，并说明用户应回到哪个区域处理。
- 优化 FA List 匹配列状态层级：未选择时使用弱提示色，已选择时使用主题主色，避免同一蓝色同时表达空状态和完成状态。
- 补齐看账工具导出防重复提交：导出线程启动前即禁用导出按钮并显示状态；导出期间重复触发会被拦截并提示等待当前任务完成。
- 优化 FA List 补充清单可选步骤提示：合并完成后的确认弹窗明确说明补充清单是可选项，跳过不会影响基础匹配结果导出。
- 优化 FA List 补充清单页面内说明：页面顶部说明和跳过按钮改为“没有补充清单可直接跳过”，与可选步骤定位保持一致。
- 统一 FA List 文件配置页反馈入口：底部反馈从“为你点赞/用户建议”改为“认可/建议”，并使用全局链接样式。
- 优化 Excel 批量合并 Sheet 选择弹窗：确认按钮去除装饰性 emoji，改为纯文本“确定合并”。
- 优化看账工具加载状态提示：状态栏的“重载中、请设置标题行、加载完成”去除装饰性 emoji，并使用主题色区分处理中、警告和成功。
- 优化看账工具导出前置校验文案：缺少 ID、科目或金额映射时，弹窗标题改为“无法导出”，并说明应在表头中标记哪些字段。
- 优化看账工具专业语气一致性：透视配置、标题行菜单、字段映射状态、“目标匹配”和“剔除/例外”分组去除装饰性 emoji，状态颜色改用统一主题色。
- 补齐 FA List 旧导出设置页防重复提交：导出线程运行期间禁用路径、格式和上一步控件，重复触发时在状态栏提示等待当前导出完成。
- 继续清理专业工具中的装饰性符号：看账工具透视配置“移除”按钮与字段区颜色改用主题色，TS2 条件筛选说明去除灯泡符号。
- 优化 FA List 主窗口前置步骤阻断文案：缺少文件、未完成合并、未完成补充清单步骤或无导出数据时，弹窗标题改为“无法配置/无法导出/无法进入该步骤”，并说明应回到哪个步骤处理。
- 优化 FA List 文件选择、列选择和旧导出设置页的前置校验文案：预览、下一步、导出路径缺失等阻断提示改为“无法预览/无法进入下一步/无法导出”，并指出应处理的区域或动作。

## 后续低成本优化优先级

### P1：防错与状态约束

- 对长耗时任务统一补齐“处理中”状态，并在处理期间禁用主操作按钮，避免重复点击。
  - 已覆盖：Excel 批量合并主流程、FA List 合并主流程、FA List 直接导出主流程、FA List 旧导出设置页、看账工具导出主流程。
- 对“未选择文件、未选择字段、无法导出、缺少依赖”等情况统一使用明确错误文案。
  - 已覆盖：FA List 文件配置页进入下一步前置校验、FA List 文件选择页、FA List 列选择页、FA List 旧导出设置页、FA List 主窗口前置步骤阻断、看账工具导出前置字段映射校验。
- 对可选步骤使用稳定状态说明，避免用户误以为必须完成。
  - 已覆盖：FA List 补充清单映射确认弹窗、补充清单页面说明和跳过后的状态栏提示。

### P2：桌面专业感统一

- 逐步移除主操作按钮中的 emoji 图标，改为纯文本或标准 ttk 控件状态，保持内部审计工具的克制风格。
  - 已覆盖：看账工具第一屏主流程、反馈入口、透视配置弹窗、科目筛选弹窗刷新/搜索、Excel 批量合并 Sheet 选择弹窗确认按钮、TS2 条件筛选说明。
- 统一反馈入口文案，例如“认可/建议”与各工具底部状态区的位置和样式。
  - 已覆盖：FA List 文件配置页底部反馈入口。
- 将仍然存在的硬编码背景色、字体和提示色迁移到 `launcher/ui_theme.py`。
  - 已覆盖：FA List 文件配置页匹配列已选状态、看账工具加载状态栏。

### P3：空间效率

- 对表格密集页面统一检查横向滚动条、列宽、底部按钮固定区域。
  - 已覆盖：FA List 导出列选择页的底部操作行与选择数量反馈。
- 对说明文字较长的页面改为折行或独立帮助区，避免挤占表格主体。
- 对工具卡片文案较长时的高度、换行和进入按钮位置继续做一致性检查。

## 验证记录

- 已执行主要 GUI 文件语法检查。
- 已检查空白和补丁格式。
- 已扫描主要窗口是否仍存在大块控件直接挂根窗口的情况。
- 已用隐藏窗口方式实例化核心 GUI，验证标准外壳可正常创建。
- `TS2.py` 验证时发现并修复过 body 容器内 `grid` 与 `pack` 混用导致的窗口启动错误。

## 已知限制

- 仓库中存在历史文件 `tools/fa_list/gui/file_and_match_config_from314.py` 含空字节，导致全仓库编译会被该旧文件中断；本轮未修改该历史文件。
- 看账工具实例化需要 `polars`，当前环境可能缺少该依赖；本轮以语法检查和布局扫描为主，未强行安装依赖。
