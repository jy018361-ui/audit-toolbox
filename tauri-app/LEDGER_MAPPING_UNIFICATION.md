# 账表映射统一方案

## 0.7 parse_date 补齐中文日期与 Excel 序列号（2026-09-03）

借款利息测算用《借款合同台账测试集》验收时暴露：内核 `parse_date` 只认分隔符与英文
月份写法，台账里两类常见写法认不出——

1. **中文日期**（"2025年5月16日""25年1月10日"）：借款模块曾有一份 `parse_cn_date`，
   定义了但 `row_date` 从未接线，属于迁移漏网。现并入内核（两位年按 20xx）。
2. **5 位纯数字 Excel 序列号**（"45662"）：日期被粘贴成数值后单元格类型是数字而非
   日期，读取侧只能拿到文本。按 20000–80000（约 1954–2119 年）区间换算（基准 1899-12-30）。

另注意 calamine 0.36 的 `ExcelDateTime` Display 打印的是**原始序列号**而非日历文本——
各读取方应像 fx/excel_merger 一样走 `as_datetime()`，不要 `to_string()` 透传
（借款利息栽过：真日期单元格全灭、利息退化平均粗算）。
## 0.6 03 号样例实测反馈的四项修正（2026-08-30）

用户拿 03 号套（SAP 导出）实测后报：自动映射漏认「会计科目」列、LLM 复核把原币币种
指给「本币」列、借贷方向指给「过账代码」、TB 混写列只挂了编码没挂名称。逐条修完，
其中**混写列双映射是对 0.4 节口径的变更**。

### 一、混写列从「只挂编码」改为「编码与名称同挂一列」

0.4 节曾明确「混合列整列映射为 `accountCode`，映射面板上不会同时出现两个角色」。
用户实测后不认可：面板一边提示「科目名称未映射」、一边数据其实读得出。改为
`fx` 的自动映射把混写列**同时挂到 `accountCode`（单值）与 `accountName`（数组）**，
面板上两个角色都看得见。下游配套：

- `fx::account_match_key` 建匹配键时只取拆出的**编码段**——混写整串进键就永远
  对不上 JE 的纯编码列（这本来就是隐性 bug，顺带修掉）；
- LLM 复核的「一列一语义」拦截（后端 `sanitize_change_list` 与前端
  `applyLedgerReviewToDict`）对这对角色加豁免：混写列上 accountName↔accountCode
  共列不算冲突；后端冲突词同样豁免——混写列的列名带「编码」是常态
  （03 号就叫「项目编码、文本/科目编码、文本」），`NOT_NAME` 挡的是纯编码列，
  不能挡这一列补挂名称。
- `ledger_mapping::suggest_roles_with_data`（TBJE／借款那条路）仍返回单角色映射，
  **没动**：TBJE 引擎取数时本来就有拆分兜底。

### 二、03 号 JE 的「会计科目」按取值补判为科目名称

03 号序时账的编码列叫「总账科目」（取值 `1001010000`），另有一列叫「会计科目」、
取值是 `库存现金-人民币` 这种名称文本。「会计科目」在别名库里是 accountCode 的别名，
按列名它只会去争编码、争输被丢，科目名称两头落空。新增
`fx::fill_account_name_from_code_alias`：accountName 空缺时，在 accountCode 的
候选列里找「拆不出编码前缀且大多含中文（均 ≥3/4、样本 ≥4）」的列按数据补判。

### 三、「本币」列的归属与过账代码不是方向

- `functionalCurrency` 别名补 `本币`／`本幣`（SAP 的本位币列就叫「本币」）；同时
  currency 角色加冲突词（金额类词条＋本位币写法）——此前「本币」列两头都认不上，
  LLM 复核硬把它指给原币币种。
- `direction` 冲突词加 `过账代码`／`postingkey` 等；复核建议另加**取值形态校验**：
  方向角色的目标列样例取值须全部是 S/H、借/贷、Dr/Cr 这类方向标志，出现过账码
  （40/50/01）就不放行。取值证据不足（全空）时维持列名判定。
- 复核提示词同步：列名只是线索取值才是判据（「会计科目」放名称文本时是
  accountName）；统驭过账码没有借贷含义。

### 四、汇兑损益「仅未实现」模式放开 JE 原币必填

用户场景：TB 里有外币信息、序时账是本位币记账（没有原币币种/原币金额列），
手动选「仅未实现」仍被必填拦住。现在该模式下 JE 的 `currency` 不再必填、
原币金额记法不再检查（后端 `validate_mapping` 与前端 `fxMissingRequired` 同口径）。
**币种已映射时原币金额记法仍要拦**：月度测算会把外币变动当 0，期初直通期末，
那是悄悄算错。该模式下 JE 本就是可选辅助——币种缺失时外币行全部跳过，
安全退化为两时点检查。

### 验证

- `fx::tests::sap03_je_gl_account_column_and_account_name_text_column`、
  `sap03_tb_combined_column_carries_code_and_name`（合成表头）；
  `fx::tests::真实03样例表头映射`（`LEDGER_SAMPLES` 指向 TBJEPBC 目录后
  `--ignored` 实测，两侧映射逐项断言）；
- `audipick::tests::复核建议把方向指给过账代码时按取值拦下`、
  `复核建议允许科目编码与名称共用混写列`；
- `fx::tests::仅未实现模式下je没有外币列时不再拦原币必填`（三种模式对照）；
- 前端 `FxAuditPage.test.ts`（混写豁免、仅未实现缺口清单）。

## 0.5 TB 余额折算收敛到一处（2026-08-30）

**这是行为变更，不是纯修复**：汇兑损益、存款利息、借款利息、FA TBJE 四个工具读
科目余额表的结果都会变。起因是给 TBJE 完整性核对做「资产＝负债＋权益」那条检查时，
04 号样例算出 20 亿的差额，顺着查出两个一直在的问题。

### 一、TB 的方向列从来没生效过（只影响汇兑损益）

`fx::direction_column` 拼的角色名是 `{prefix}Direction`，而 prefix 带着币种口径，
于是拼出 `openingFunctionalDirection`——真实角色名是 `openingDirection`，永远匹配不上。
原币与本位币共用同一个方向列，前缀必须先剥掉口径后缀。

「净额＋方向」形态的余额表（TB2／TB5）因此一直是翻着号算的。修复后实测：
02 号样例的 TB 自身勾稽从 99 行不平降到 8 行，08、09 号从有差异变为全部通过。

另外三个工具拼的是 `openingDirection`，角色名一直是对的，没踩这个坑。

### 二、余额列自带符号时被方向列多翻了一次（四个工具全中）

04／05 号样例的余额列自带负号（应付票据期初 −28,138,279.04），旁边还并排一个写着
「贷」的方向列；而**同一张表的发生额列是正数**。符号口径靠勾稽等式投票判定，
投的是发生额列的口径，只能判出 unsigned——拿它去折算余额，负债和权益整片翻正，
会计恒等式差出两倍资产。

新增 [`ledger_mapping::balance_self_signed`] 单独判余额列：**方向列写着「贷」的那些行里，
余额是负数的占多数**，就认定整列自带符号、方向列只是冗余标注。折算走
[`ledger_mapping::signed_balance`]。

两条约束值得记住：

- **必须整列判，不能按行看「这个数是不是负的」。** 08 号样例是标准的「绝对值＋方向」，
  但个别科目有异常余额（方向记贷、数值为负）；按行判会把这些行漏翻，合计差出一亿七。
- **这条规则只对余额列成立。** 序时账的贷方行记 −50 表示冲掉之前那笔贷 50，必须乘 −1
  得 +50 才抵得平；发生额与凭证金额仍走 `signed_amount`。

### 三、四份折算收敛成一处

四个工具此前各拼各的 `AmountInputs`，把修复放进 `fx` 等于又开第五份口径。现在判定与
折算都在公共引擎，四个调用点共用同一份判据：

| 工具 | 折算入口 | 本轮改动 |
|---|---|---|
| 汇兑损益 | `fx::signed_amount`（余额分支转调 `signed_balance`） | 方向列角色名 + 余额符号 |
| FA TBJE | `fa_tbje::balance` | 余额符号 |
| 借款利息 | `loan_interest` 的 opening／closing | 余额符号 |
| 存款利息 | 新增 `deposit_interest::tb_balance` | 此前**完全不看方向列**，直接取净额原值——货币资金是借方余额，它自己的场景基本无害，但贷方余额的科目（如银行透支）会少一个负号；现在统一到同一入口 |

### 验证

十套真实样例上，会计恒等式（全类别余额加总应为零）：**02、04、05、06 两份、08、09、10
共八份精确归零**；01 差 5.99 亿、03 差 7.40 亿、07 差 30,820 未收敛，尚未判明是工具的问题
还是账本身的问题。TB 自身勾稽七份通过。

回归命令见文末；真实样例的逐份结论用
`cargo test --lib 真实样例的三条核对 -- --ignored --nocapture` 打印。

## 0.4 十套真实 TB/JE 样例驱动的一轮加固（2026-08-30）

样例来自 `TBJEPBC` 目录的十套账（每套一份科目余额表 + 一份序时账，06 套按期间拆成四个文件）。
逐份扫过表头与前若干行后改了八处。**这些结论来自这十套的形态，不等于覆盖了所有 ERP 导出格式。**

**一、末级科目判定（`tb_leaf_mask`）从「只认编码前缀」扩到三条互补路径。**

原实现只按同一主体内的科目编码前缀判父子，十套里有五种形态它够不着，且都是**静默算重**：

| 形态 | 样例 | 原先的结果 |
|---|---|---|
| 父行与辅助核算明细行**编码完全相同**（`1121.01` 既是汇总行也是每个客户的明细行） | 01 | 父子都留 → 翻倍 |
| 明细行是核算维度行、**科目编码列为空**，父行才有编码 | 06、10 | 空编码行一律保留 → 翻倍 |
| 小计行写在明细**下方**且编码列为空 | 04、05 | 同上 |
| 父科目行与 `xxx-小计` 行数值重复、同时存在 | 06 | 双计 |
| 编码与名称混写在一格，前缀比不出上下级 | 03 | 父子都留 |

现在三条路径并用：编码前缀（加了**分隔符边界**校验，`1002.1` 不再被当成 `1002.10` 的上级）、
合计标签、**金额勾稽**。勾稽是新增的核心——某行在所有已映射金额列上都等于相邻连续若干行之和时，
剔除其中**没有科目编码的那一侧**：核算维度明细行没编码、对不上序时账，留有编码的父行；
反过来小计行没编码而明细行有，就删小计行。

勾稽有两条守卫，都是**在真实样例上抓到误删后**加的：

- **跨币种不勾稽。** 02 套有一批科目的 CNY 行与 USD 行四个金额列数值完全相同
  （`1002011802` 两行都是 182,699,025.78），差别只在方向列一个记贷一个记借——只比金额会把 CNY 行判成 USD 行的汇总。
  只用币种列判，**不能用方向列**：04/05 套小计行的方向列是空的，拿方向来卡会把那边真正的小计行放过去。
- **只累加一行时，要求两行编码相同或确有上下级关系。** 「A = B」这个证据太弱：一借一贷金额相同在会计里是常态，
  02 套的 `交易性金融资产-成本` 12 万借方与 `交易性金融资产-公允价值变动` 12 万贷方是**两个平级科目**，四个金额列一模一样。
  加这条之后顺带修正了一处过度剔除：03 套的 `5302` 是级次 1 但**没有任何末级子科目**，它自己就是末级，旧口径把它连余额一起剔掉了。

必须放过的反例也覆盖了：02 套同一编码按 CNY/EUR/USD 拆三行，谁也不等于另两行之和，勾稽不触发，三行都留。

**二、噪声行剔除（`ledger_junk_mask`，新增）。** 两条判据，都不依赖「合计」二字：

- 身份列（凭证号／科目编码／科目名称／日期）全空但金额有值 → 02 套序时账有三行这种，**两行埋在二十多万行的表体中间**；03 套是 SAP 的 ALV 分组小计，每个科目两行，标签写在没映射的列里，名称列是 `#N/A`。
- 从表尾往前扫到第一个有身份的行为止 → 10 套序时账合计行后面跟着十五行手工草稿（末行 `#REF!`），09 套合计行后面是「制单人／打印时间」页脚。**只从表尾扫**：06 套的 `-小计` 在表体中间，从那里截断会丢掉后面整段账。

「合计」关键词只用作**整格精确匹配**（外加 `-小计` 这类带连接符的后缀）：10 套里 `试剂耗材合计` 是真实末级科目名，01 套序时账的摘要里大量出现「合计：2010.00」，按包含判都会误杀。

**三、正表 Sheet 选择改为规模主导。** 十套里**六个文件**塞着审计人自建的透视／核对表，
且右半边整块粘着对应余额表的副本——那半边就是标准 TB 表头，表头分不比正表低。
原先大文件路径**完全不看行数**，小文件路径的行数权重一千行就封顶。现在规模按对数计权且权重压过表头分常见差距，
表名带「透视／核对／check／修改」等标记再降一档（只降权不排除，防止用户把正表就叫「核对表」）。

**四、科目编码匹配加前导零归一。** 05 套序时账约一成科目补零到十位（`0000943100`），
同套余额表一个前导零都没有——原先这批科目在 TB↔JE 之间对不上，且不报错，只是安静地少算。
归一只用于**建匹配键**，展示值仍按账里原本的写法。

**五、别名补齐。** 按实测表头补：`科目`（04/05 的明细编码列，旁边另有「科目级别」放一级编码）、
`总帐科目`（「帐」的异体写法，TB 表此前只收了「账」）、`累计余额`（02 的期末列）、
`借方累计`／`贷方累计`（08/09 的词序与既有别名相反）、`借贷标志`（04 的 S／H 方向列）、
`唯一码`（07 已拼好的凭证键）。同时给科目编码补了对手方冲突词——`抵销科目`（03）、`统驭科目`（02）、
`对方`／`往来`／`预算`：收了裸的「科目」之后这些列会跟着命中，而它们的取值跟本方科目长得一模一样。

**六、编码与名称混写在一格时就地拆分。** 03 套是刚需：整张表只有一列科目，一级写作 `1001/库存现金`，
末级写作 `1001010000:库存现金-人民币`，**没有独立编码列**；而它的表头 `项目编码、文本/科目编码、文本`
同时带着「科目编码」和「文本」，冲突词一票否决，按列名永远落不到科目编码上——只能按数据认：
整列四分之三以上的取值能拆成「编码+名称」，这一列就是科目列。

分隔符只认 `/ : _ \ |`，**不认 `-` 和空格**（`银行存款-人民币-中国银行`、`应付账款 - 应付暂估款` 是名称自己的分段），
首段还必须像编码（含数字的 ASCII 串）——这一条把 06/10 的 `交易性金融资产_结构性存款`、`管理费用_研发费用_水电气费`
这类**层级名称拼接**挡在外面。08 套反过来：名称列写成 `10020101\银行存款\在财务公司存款\活期`，
取名称时要把编码前缀切掉，否则和序时账那侧的纯名称对不上。

**空格边界的例外（2026-09-02 修订）**：`6701090001 财务费用-汇兑收益` 这类「编码＋空格＋名称」
（用友导出、审计底稿常见）原先整格当编码，过不了正文行的编码语法校验，整批正文行被当垃圾剔掉
（fx 损益取数返回空集即此回归）。现在首 token 本身是**足位数**（≥3 个数字）的 ASCII 编码时，
空格也认作编码／名称边界；首 token 不是编码（`应付账款 - 应付暂估款`、`3 个月定期`）时仍绝不拆。
`-` 仍然永远不当分隔符。

**没有**改「一列只承载一个角色」这条约束：混合列整列映射为 `accountCode`，名称在取值时拆出来，
映射面板上不会同时出现两个角色。（0.6 节起 `fx` 侧改为编码与名称同挂一列，见彼处说明。）

**七、币种线索列改为按取值挑，不按列名挑。** 原先按列名判在先，判出结果后按数据判的那段直接跳过——
04/05 的 `科目级别描述` 只凭「描述」二字就抢走了线索列，可它整列都是 `1002_银行存款` 这种一级科目名，
一行都抽不出币种；真正带「美元户」的明细名称列因为角色已被占，再没有机会（线索列是单列角色，抢错无补救）。
现在按列名判出的结果一律作废，改在所有列里数「真能抽出币种」的行数，取最多的那列；平手取更靠后（更明细）的；
**一列都抽不出就让它空着**。已判为原币／本位币币种代码的列不参与——那两列整列都是币种代码，命中率必然百分之百，
而线索列的意义恰恰是「没有币种列时的退路」。币种词表同时从 `fx.rs` 搬进公共引擎，识别与取值共用一份，
不再可能出现「识别时认定这列有币种、取值时又抽不出来」。

**八、LLM 复核提示词跟着改口径。** 混合列此前被「accountCode 与 accountName 绝不能映射到同一列」
一句堵死，现在明确：整列映射为 accountCode，引擎会拆名称，既不要改判成 accountName 也不要同时填两个角色，
并写清与层级名称拼接的区别。币种线索列改成「只看取值不看列名，抽不出就空着」。
另点名 `抵销科目`／`统驭科目`／`对方科目`／`往来科目`／`预算科目` 不是本方科目，
并把 `总帐科目`、`借贷标志`、`唯一码`、`借方累计`、`累计余额` 补进常用表头示例。

### 真实样例上的验证结果

两条调查用测试都对这十套跑过（命令见下）。**22 份文件的形态全部完整命中**（TB1/TB2/TB3、JE1/JE2/JE3），
其中 03 套此前落不到科目编码、形态判不出，04/05 套的 `科目` 列此前认不出来。

末级科目与噪声行的剔除结果，能对上外部事实的几项：

| 样例 | 总行 | 计入 | 剔除 | 对照 |
|---|---|---|---|---|
| 01 TB | 3533 | 3296 | 237 | 剔除的都是 `1002`／`1002.01`／`1002.01.01` 这类父行 |
| 02 TB | 372 | 371 | 1 | 只剔那行游离数字；多币种拆行与平级同额科目都保住了 |
| 03 TB | 290 | 245 | 45 | 级次 1 共 46 行，其中 `5302` 无末级子科目应保留 → 45 正确 |
| 05 TB | 503 | 451 | 52 | 与人工清点的「451 行明细 + 52 行汇总」完全一致 |
| 06 TB(1-3) | 587 | 122 | 465 | 其中 409 行是核算维度明细行，与人工清点一致 |
| 10 TB | 536 | 131 | 405 | 其中 375 行核算维度行 |

### 尚未处理

- **06 套两个分段文件是两种导出格式**：列数 22 vs 44、列序不同、借贷金额列位置不同、`币别` 列一有一无，
  且 4–12 月那份的凭证头字段只在每张凭证首行有值（约 85% 行为空，需向下填充）。01 套序时账的日期列同样是稀疏填充。
  **本轮没有做凭证头向下填充**，这类文件仍需用户自己先补齐再导入。
- **09 套两个 `.xls` 整表是文本**，金额带千分位逗号、月日带前导零；`parse_amount` 认得千分位，
  但「月/日两列拼不出年份」（08/09 套序时账都没有完整日期列，年份只写在标题行里）没有处理。
- **02 套同编码多币种拆行**只验证了不被误删，没有验证下游按币种汇总的口径。
- 上述结论都来自合成回归与表头形态比对；**除映射调查测试外，尚未用这十套跑完整的端到端出表验收**。

### 回归

```
cargo test --manifest-path src-tauri/Cargo.toml --lib
LEDGER_SAMPLES="<样例目录>" cargo test --manifest-path src-tauri/Cargo.toml --test mapping_survey -- --ignored --nocapture
LEDGER_SAMPLES="<样例目录>" cargo test --manifest-path src-tauri/Cargo.toml --lib 真实样例的末级科目 -- --ignored --nocapture
```

第二条看**表头落到哪个角色**，第三条看**哪些行被算进来**——后者才是会静默算错数的那一半，
此前没有任何调查手段，本轮补上（`fx.rs` 内的 ignored 测试）。两条都依赖本机样例目录，默认不跑。

十套样例的真实表头写进了 `ledger_mapping.rs` 的单元测试（角色归属、混合列拆分、末级科目的五种形态与两个反例、
噪声行两条判据），`fx.rs` 里有 Sheet 选择与币种线索列的用例，`fa_tbje.rs` 里有前导零归一的用例。不在文档里写死数量。

## 发布前回归补齐（2026-08-28）

- `useLedgerDictReviews` 按 TB/JE 分别维护请求版本：开始重新识别、清空、路径/Sheet/表头变化、卸载均使旧复核失效。过期请求既不能回写映射和提示，也不能解除新请求的锁定；失败不把旧映射重新应用。返回值仅含仍有效的结果，汇兑损益后续跨表校验也用相同版本守卫。
- 存款分类请求新增 `accountRoleOverrides`，只含用户手选项；`accountRoles` 仍携带完整展示预设。默认 `excluded` 不再挡住手选父科目向自动未识别末级的继承，用户明确排除的末级仍优先；没有新字段的历史任务继续按原 `accountRoles` 处理。重新识别后清掉旧手选项并提示重新核对，不把旧文件决定带进新文件。
- 回归：`npx vitest run src/components/LedgerReviewAll.test.tsx src/DepositInterestInteractions.test.tsx src/DepositInterestPage.test.ts src/DepositInterestLinks.test.tsx src/FxAuditPage.test.ts --exclude='**/.claude/**'`；`cargo test --manifest-path src-tauri/Cargo.toml --lib deposit_interest::`。前端使用真实 React 生命周期与延迟 mock 响应，不调用外部 LLM；Rust 合成样例同时传全部默认角色与手选角色，覆盖父项继承和明确排除。

> 状态：**已落地**。方案撰写于 2026-08-22，代码于 2026-08-23 完成。
> 统一内核在 [`src-tauri/src/ledger_mapping.rs`](src-tauri/src/ledger_mapping.rs)，
> 五个工具全部改为调用它。2026-08-24 按金标（TB-4800 的两个类型表）做了第二轮对齐，见 §0.1。
> 测试命令见下方「回归测试」，不在文档里写死数量。
> 目标：把汇兑损益、存款利息、借款利息、看账、正负数凭证标记这五个工具的
> TB/JE 字段映射，从四套各自为政的实现收敛成一套。

## 0.3 主体降为选填，映射面板改按型号组织（2026-08-29）

两处都是「设计早就定了、界面没跟上」的落差，起因是一轮当面复核。

**一、`entity` 是选填，界面却当必填拦。**
金标 2026-08-24 就把主体从 required 降为可选（`identity_required` 里没有它），
但汇兑损益前端把「公司/核算主体（或固定主体）」列进缺失清单、后端另有两条
「缺少主体列时必须指定固定主体」的阻断，FA TBJE 还要用户在识别步骤手填主体名。
三处全部拆除，改为：

| | 做法 |
|---|---|
| 没映射主体列 | 全表统一挂 `DEFAULT_ENTITY`（「默认主体」），不提示不阻断。Rust `fx::fixed_entity` 与前端 `ledgerMapping.DEFAULT_ENTITY` 同一个名字 |
| 映射了主体列 | 按列里的主体走，与原来一致；汇兑损益单主体时沿用该主体名做底稿封面 |
| 界面 | 汇兑损益那两个格子（固定主体名 ＋ 本位币）收敛成一个「本位币（全表）」——**真正必填的一直是本位币**，主体只是它的挂载点；FA TBJE 的输入框保留但改为选填，留空按默认主体 |

**二、下拉分组按字段类别切，看不出「这几个格子为什么突然必填」。**
分组标题此前写死「本位币金额　必填，三种记法选一种」，与当前命中哪一型无关。
现在改为按**当前命中的型**组织，五个工具共用：

- 新增 `ledger.forms` 下发 TB 六型／JE 三型的槽位定义（台账那份仍随 `loan.inspect` 下发），
  前端 `src/ledgerForms.ts` 只做纯计算，与 Rust `match_forms` 同一套排序规则与次选口径
- 面板顶部给一句结论：「已识别为 TB-类型C（本位币借贷分列）」／「最接近 …：缺少「本年累计借方发生额」」
- 分组顺序：身份类 → 这一型的各槽位 → 本期发生额（次选口径）→ 其他记法（这一型用不到，但仍可选）
- 必填标记逐项跟着型走（`＊`／`（选填）`），借款台账早就是这么做的，这次把 TB/JE 接上

**型号名一律用字母**：`Form` 新增 `display` 字段，`TB1..TB6` → `TB-类型A..F`、
`JE1..JE3` → `JE-类型A..C`、台账 `A..D` → `台账-类型A..D`。
**`id` 保持不变**——代码、测试与金标的那两张类型表都在用它，改了对不上。

**三、顺带修掉一个表头回归。** 同期新加的「两行表头作为一个候选整体参与竞争」
（`fx::infer_header_layout`，为的是别让 `借方/贷方` 那一行单独胜出）没有区分
**分组表头**与**标题行**：`序时账` 这种只有一格有字的标题行也会被并进表头，
`merge_headers` 把它顺延到每一列，列名全变成「序时账-公司代码」。
后果有两个——映射标签被污染，以及汇兑/存款读到合并表头、看账那侧仍按单行读，
两边表头对不上（`ledger_engine_parity_tests` 因此转红）。
现要求**上一行至少两格有字**才作为分组表头参与配对，标题行照旧跳过。

回归：`npx vitest run src/ledgerForms.test.ts src/FxAuditPage.test.ts src/FaTbJePage.test.ts`；
`cargo test --manifest-path src-tauri/Cargo.toml --lib`。

## 0.1 第二轮：按金标对齐（2026-08-24）

`汇兑损益测试资料/TB-4800.xlsx` 里有两个此前没被用上的工作表——`tb种类`（六型）与
`je种类`（三型），逐格标注了每个角色是 required 还是 optional、哪个可多列。
**这两张表提升为账表类型定义的正式来源**，与内核逐格比对后做了四处改动：

| 改动 | 说明 |
|---|---|
| 收编三个游离角色 | `auxiliary` 辅助核算（可多列）、`loanId` 借款明细、`period` 会计期间（仅 TB）。此前汇兑损益靠一行 `role == "auxiliary"` 特判、借款利息按关键词临时找，都已拆除 |
| 必填改为并集 | `missing_required` 现在返回 **金标身份槽 ∪ 金额形态槽 ∪ 工具自己声明**，每条带 `from_gold` 标明是谁在要求。前端 `missingGoldIdentity` 是同一份定义 |
| JE 形态优先级修正 | 排序规则是「同为完整命中时后定义的型优先」，而 JE 数组原本正序排列，导致纯净额型排在方向型前面。数组倒置后 4800/3300 两份序时账正确判为 JE2 |
| 本期发生额作次选口径 | 金标只写了本年累计，但实测「科目余额表.xls」（用友）只给本期发生借贷。本年累计缺失而本期借贷齐全时放行，与汇兑损益前端既有的「本年累计（或本期）」口径一致 |

**`auxiliary` 的别名只收汇兑损益那一份**：存款利息另有一份把「文本／科目文本／账户文本」
也算辅助核算（它靠这些列认存款档次），并进标准表会把科目名称抢走。工具需要额外写法时
在自己那边追加，标准表保持保守。`loanId` 同理只认「合同编号／借款编号／借据」这类特异写法，
`辅助`、`明细`、`客户` 这类泛词留给 `auxiliary`。

**这是一次实质收紧**：金标要求 JE 的记账日期、凭证识别字段、科目编码、科目名称、摘要齐备，
TB 要求科目编码与名称齐备——缺一项就拦。今天能跑的账里，缺摘要列的、只有科目编码没有名称的，
改造后都会被拦下。`entity` 不在其中（金标 2026-08-24 修订时降为可选），汇兑损益仍自己要求它。

## 0.2 第三轮：引擎收敛（2026-08-25）

第二轮只统一了角色词汇表，往上每一层仍各写各的。这一轮把剩下四层收敛完：

| 层 | 做法 |
|---|---|
| **LLM 复核规则** | 看账那份独立 JE 规则删除，改用 `REVIEW_COMMON` ＋ `REVIEW_JE`。库里只剩 TB、JE 两份规则，工具能用哪些角色由 payload 的 `availableRoles` 声明 |
| **卫生过滤** | 汇兑损益那套（6 条规则）提升为共用，并入看账新加的冲突词否定；同时支持 `changes` 与 `fills`／`reviews` 两种输出结构 |
| **复核入口** | 新增 `ledger.review_mapping`。**存款利息与借款利息第一次有了复核**，映射面板上各加一个按钮。2026-08-27 起存款利息与汇兑损益改为**一键复核 TB＋JE**：两个文件只留一个按钮，共享引擎在 `applyLedgerReviewsTogether`（`src/ledgerMapping.ts`），两边并行、结果各自汇报，单个失败不阻塞另一个；借款利息仍是单文件按钮 |
| **角色名** | 看账的 `amount`／`debit`／`credit` 统一到 `functionalAmount`／`functionalDebit`／`functionalCredit`。Rust 侧只改 serde 线上名并留 `alias`，28 处代码引用不动 |
| **前端映射面板** | 四套合成 `components/MappingPanel.tsx` 一套。差异降级为参数：角色清单、分组标题、可多列角色、方案互斥锁定 |
| **表格读取** | 看账与正负数标记接上 TS 那套稳定 Parquet 缓存；借款利息的 TB／JE 委托给共用读取 |

**新增能力：按科目编码段批量筛选。** 科目清单改为返回「显示值 ＋ 编码」的结构化数据，
`kanzhang.accounts` 接受 `codePrefixes`。填 `6401,6603` 圈出这两段下的全部明细科目。
只比编码段、不比整个拼接串——否则 `2241110001-其他应付款-6401专项` 会被误拉进来；
也不能按第一个连字符切开显示值，Oracle 的段式科目编码本身就含连字符。

### 两处原判断的修正

计划里写「借款利息那第三套读取没有独特需求，直接删除并入」——**这是错的**。
共用读取的标题行探测内置了账表的语义先验（按科目、金额、日期这类词打分），
拿它扫借款台账会把数据行当成表头：实测「08 星衡科技集团-票据及短期融资登记簿」
被判到第 9 行，整张表识别不出一笔借款。现按表的种类分流——**TB／JE 走共用读取，
台账保留本模块的 `detect_header`**。

计划里还写「看账有 Parquet 稳定缓存」——那是 TS 工具的，看账走的是裸读取。
接上缓存后实测 36 万行序时账：**首次 44.4 秒，再次 6.3 秒，提速 7 倍**
（debug 构建）。验收命令：

```
LEDGER_SAMPLES=<样例目录> cargo test --manifest-path src-tauri/Cargo.toml   --test mapping_survey bench_ledger_cache -- --ignored --nocapture
```

### 币种一律分两列（2026-08-25）

金标的两张类型表这天补齐为 `原币currency` ＋ `本位币currency`——TB 本来就分两列，
JE 那栏此前只写了一个 `currency`。核对代码后确认**内核一直是分两列的**
（JE 与 TB 都有 `currency` ＋ `functionalCurrency`，汇兑损益也逐行在读本位币币种列），
但有两处没跟上：

| 问题 | 说明 |
|---|---|
| **JE 面板选不到本位币币种** | `JE_LABELS` 里没有 `functionalCurrency`，映射面板的「币种」组只列了一项。后端认得、界面给不了——序时账上用户根本没法指定本位币币种列，只有科目余额表能选。已补 |
| **同一角色两套中文标签** | `currency` 在 JE 叫「交易币种」、在 TB 叫「原币币种」。已统一为「原币币种」 |

`functionalCurrency` 的标签同时从「本位币」改为「**本位币币种**」：它存的是 CNY／USD
这类三位代码，原来的叫法容易和「本位币金额」混淆。LLM 的 JE 币种段也按两列重写，
点明两者都是**币种代码列**而非金额列，并补上只有一列时按取值分布判定的规则。

实测 SAP 序时账（`JE+YTD+OCT.xlsx`）：`Document Currency Key` → 原币币种、
`Company Code Currency Key` → 本位币币种、`Group Currency Key` 正确排除。

### LLM 复核实测（2026-08-25）

对 `汇兑损益测试资料` 逐份跑真实复核（跳过三份几十万行的大文件，表头映射用不到它们）。
第一轮跑出两条错误建议，都不是模型"笨"，而是**我们没把该给的信息给它**：

**一、没告诉模型脚本已经判出什么形态。**
`序时账-1.xlsx` 既有账里导出的原始「金额」列，也有客户用 `=IF(方向="借",金额,-金额)`
算出来的「借正贷负」列。模型看到金额列全是正数，就以 0.95 的置信建议改指过去。
它不知道脚本已经判定 **JE2（方向＋净额）完整成立**，更不知道在 JE2 下净额列
有没有正负号根本不影响结果——**符号口径由数据配平判定，不由列名判定**。

送给模型的 payload 里只有表头、样例和当前映射，**没有形态结论**。现在补上
`currentForm`（型号 id、标签、是否完整命中、缺哪些槽），并在通用纪律里写死：
complete 为 true 时构成该形态的槽位一律不要动，**两种映射都能成立时维持现状**。

**二、集团货币的排除对序时账从来没生效过。** 这是个既存缺陷。
模型建议把本年累计贷方指到 `MTD Grp Curr`，理由里自己都写着「为集团货币口径」。
查下去发现两个洞：

- 判断"是不是金额角色"用的是 `contains("Functional")`，**大小写敏感**。角色名有
  `functionalAmount`（小写开头）与 `ytdFunctionalCredit`（驼峰中段）两种写法，
  **JE 的三个金额角色一个都匹配不到**。一份只给集团货币金额、不给本位币金额的
  序时账，会把集团货币当本位币收下。已抽成 `excludes_group_currency` 并改为大小写不敏感。
- 排除词表里有 `groupcurr`，但 SAP 实际写的是 `Grp Curr`（`MTD Grp Curr`、
  `YTD Act (Grp Curr)`），匹配不到。已补 `grpcurr` / `grpcurrency`。

两处修完重跑：**五份样例全部「无修改建议」**，脚本的形态判定一个没变。

> 一度加过一条"列名带 `借正贷负`／`唯一码` 就拦"的黑名单规则。**已撤除**——
> 那是对着症状打补丁，把信息给足之后模型自己就不提了。留着反而会误伤
> 那些净额列本来就叫「借正贷负」的账。

运行命令（会调用外部 LLM，产生费用）：

```
LEDGER_SAMPLES=<样例目录> cargo test --manifest-path src-tauri/Cargo.toml   --test mapping_survey survey_llm_review -- --ignored --nocapture
```

### 04 PBC：整列同码的凭证货币列 + 「复核完成，无需调整」（2026-08-29）

`TBJEPBC/04JE.XLSX`（164,420 行 × 61 列，8MB 以上走样本识别）跑出一组连环问题：

**一、整本账都是本币业务时，「货币」列整列 CNY。** 按取值形态（填满＋单一代码 → 本位币列）
它被建议映射到 `functionalCurrency`，`currency` 被挤空；「凭证货币金额」列名里「凭证金额」
不是子串（中间隔着「货币」二字），`foreignAmount` 也拿不到别名通路。于是必填清单一直挂着
「尚未映射：原币币种、原币金额方案」。

**二、复核不但没救回来，还替脚本拍了胸脯。** `currentForm` 注入的是 JE2（方向＋本位币净额）
complete=true——公共形态表里 `currency` 根本不在槽位上（它是 FX 工具自己的必填）；
提示词又写着「整列同一个代码且几乎不空的是 functionalCurrency」，模型按纪律维持现状，
结论「复核完成，当前映射无需调整」与画面上的红色缺失清单直接矛盾。

修复分四层：

| 层 | 改动 |
|---|---|
| 脚本判定 | **序时账上列名证据优先于取值形态**（仅 exact/segment 级命中）：凭证货币命名（货币／凭证货币／Document Currency）的列即使整列同码也归 `currency`；没有本位币命名的列仅凭整列同值不再抢位。**TB 维持形态裁定不变**——TB 的「货币」列整列同值登记的确实是主体本位币（4800 有回归测试）。故意不含 partial 级命中：「集团货币」只含弱命中，若按弱命中豁免会被误收 |
| 别名库 | `foreignAmount` 补「凭证货币金额／憑證貨幣金額」；`functionalCurrency` 补「总账货币／總賬貨幣」（JE 与 TB 两处），SAP 中文导出的 Ledger Currency 不再因「含货币」去抢 `currency`；`accountCode` 补「总帐科目／總帳科目」——**「帐」是「账」的旧异体字**，04 PBC 表头就写作「总帐科目」，此前脚本认不出、科目编码全靠映射校验的口径核对兜底纠正 |
| 复核提示词 | JE 币种段改为「只有一列时先看列名」：凭证货币命名 → currency（整列同码只是没有外币业务的正常形态），本位币命名 → functionalCurrency，列名两头不沾才按取值分布判；通用纪律点明 **complete 只说明形态自身槽位成立，不代表整表映射完备**，availableRoles 里还缺的角色必须照样补齐 |
| 前端结论 | 一键复核的结论必须与「尚未映射」清单一致（`LedgerReviewSlot.missingAfter`）：仍缺必填时说「LLM 未提出调整建议；仍有未映射：…请手工指定」，不再说「当前映射无需调整」。汇兑损益／存款利息／账表核对三处接线。另：大文件样本识别的「资产负债表日不再自动带出」提示行按用户反馈删除（属噪音） |

回归：`fx::tests::je_document_currency_column_keeps_currency_role_even_when_uniform`
（合成 04 PBC 表头，含「总帐科目」异体字断言）；4800 的 TB 形态裁定、`序时账-1.xlsx` 的
币种建议均未受影响。修复后对真实 `04JE.XLSX`／`04TB.XLSX` 直接跑表头识别验证过：
JE 侧 `currency→货币`、`foreignAmount→凭证货币金额`、`functionalAmount→本位币金额`、
`functionalCurrency→总账货币`、`accountCode→总帐科目` 全部自动映射，JE2 形态完整命中；
TB 侧与修复前逐角色一致（`functionalCurrency→货币` 不变）。

## 0. 落地状态

统一内核 [`src-tauri/src/ledger_mapping.rs`](src-tauri/src/ledger_mapping.rs) 提供六件事，
四套各自为政的实现全部换成它：

| 内核能力 | 对应章节 |
|---|---|
| 角色词汇表（别名库＋冲突词库） | §3.2／3.3／3.12 |
| 币种列判定与本位币反判 | §3.4 |
| TB 六型／JE 三型的整组匹配 | §3.5 |
| 借贷符号方向判定 | §3.6 |
| 余额与发生额的统一折算 | §3.6 |
| 旧角色名迁移（历史映射仍可读） | §5 |

| 工具 | 改了什么 |
|---|---|
| **看账 / 正负数** | 表头识别与符号口径判定都换成内核。**符号判定这套本来就出自看账**——五个工具里它对序时账的判定最完整，提升为公共内核后其余四个工具共用 |
| **汇兑损益** | 角色表换成内核；借贷方向由 `foreignDirection`＋`functionalDirection` 合并为共用的 `direction`；TB 新增本年累计发生额与期初/期末方向列；映射校验里加了形态匹配提示 |
| **存款利息** | 角色表换成内核；**科目编码与名称拆成两个角色**（旧版混在一个多选的 `account` 里）；发生额改为「本年累计优先、本期次选」；`period` 收敛为 TB 专属角色，序时账侧不再放行「只映射会计期间」 |
| **借款利息** | 表头识别换成内核；期初/期末本金改由内核按六种 TB 形态折算，不再假定客户一定给贷方本金列 |

**回归测试**：9 家公司的真实科目余额表与序时账（用友／金蝶／SAP／Oracle EBS，含繁体、
段式科目、双语表头）共 18 份表头固化在 `ledger_mapping.rs` 的 `TB_FIXTURES` / `JE_FIXTURES` 里，
逐列比对期望角色。新收到一种形态就往里加一条。

**多列同名时按金额量级消歧**：一张余额表同时给「本期发生」和「本年累计」两组、
却都只写「借方发生额」的情况很常见，列名分不出来。本年累计覆盖的期间更长，合计一定不小于本期，
抽查若干行比大小即可（`MAGNITUDE_SAMPLE_ROWS` 行内至少 `MAGNITUDE_MIN_VALUES` 个有效数值才下结论，
量级持平就维持别名判定）。**集团货币／报告货币是第三套口径，整体排除在金额角色之外**——
SAP 那份 TB 的集团货币数字大出七倍，拿它跟本位币比量级会把本年累计判到错的那一列。

**方向列不是独立必填**：它只在某一型内部与净额成组出现。没有方向列时命中「净额」型（TB1／TB4），
有方向列才命中「方向＋净额」型（TB2／TB5），两种都不报错。序时账同理——借贷分列本身已表达方向，
JE1 不要求方向列。

**两处按真实数据改掉的设计**：

1. **JE1 不再要求方向列**——9 份序时账里借贷分列的有 6 份，没有一份带方向列。
   借贷分列本身已表达方向，有方向列时只作校验。
2. **没写「本期／本年」的发生额一律按本年累计**——实务表大多直接写「借方发生额」，
   审计导出的是全年数。只有明确写了「本期」的才落本期口径。

**24 文件测试集验收（2026-08-23）**：`C:\Users\lenovo\科目余额表与序时账测试集`（9 组虚构公司
TB＋JE 配对、用友 UFPrn 真实导出、SAP 风格 47 列大表 ×2、SAP 形态 A 余额表 ×2）逐份跑
`mapping_survey`。coding 侧修了三处后 22 份全部准确：JE summary 补「文本」别名（SAP SGTXT）
并以「抬头」冲突词挡住凭证抬头文本；汇兑损益的 auxiliary 允许多列并补繁体别名
（往來單位／客戶／供應商／counterparty）；inspect 输出 `formMatches` 让映射面板在测算前
就能看到形态完整性。20/21 形态完整命中（10-科目余额表只有本期发生无本年累计，属数据本身，
提示用户换全年累计版即可）。LLM 复核经三轮提示词迭代＋`sanitize_mapping_changes`
五道卫生过滤（列名真实性、置信 ≥0.5、复述现状、目标列占用与挪移链、reason 否定结论）
后 21/22 无噪声建议；仅剩的输出是 reason 自洽的可辩建议（公式列 vs 原始列的取舍），
属复核正常产出。过滤的每条规则都源自实测模型行为，均有对应单测。

**多列角色的禁入词与读取口径补齐（2026-08-23）**：真实 4800 序时账暴露两处误并——
「冲销凭证号」包含"凭证号"被并进凭证键，「预算二级科目描述」包含"科目描述"、
「对方科目名称」包含"科目名称"被并进科目名称。处理分三层：

1. **内核冲突词**：`id` 角色补冲销／沖銷／反冲／reversed 等，`accountName` 的
   NOT_NAME 补预算／对方——所有走内核的工具（看账／正负数／借款利息直接取内核结果，
   汇兑损益取内核角色表）同时生效；
2. **LLM 提示词**：汇兑损益的 JE／TB 复核提示词与看账映射提示词写明
   "多列仅限真正的拆分：科目名称只并自己的层级列，凭证号只并构成凭证键的列"；
3. **工具侧口径**：存款利息的候选打分原本把冲突词当扣分项（0.35／条，照样过门槛），
   已改成与内核一致的一票否决；借款利息 TB＋JE 模式的读取原本按旧角色名直查
   （account／debit／credit），内核新名映射下一行都进不来，已改为按标准名经
   旧名迁移读取，科目文本＝编码＋名称拼接、旧单格 account 仍可读。

回归用例：`cargo test --manifest-path src-tauri/Cargo.toml ledger_mapping::`
（冲销凭证号与预算对方科目不得混入多列角色）、`loan_interest::`
（tbje模式按内核新名读科目与金额／tbje模式旧名映射仍然可读）。

## 1. 为什么要做

五个工具都要让用户把 Excel 表头对应到业务字段，做的是同一件事，代码却有四套。

| 工具 | 读表 | 候选打分 | 角色定义 | 校验 | LLM 复核 | 前端界面 |
|---|---|---|---|---|---|---|
| 汇兑损益 | 自有（含大文件轻量路径） | 完整 | JE 17 ＋ TB 21 | 完整 | 有 | `FxPreview` |
| 存款利息 | 复用汇兑损益的 | **复制粘贴自汇兑损益** | JE 11 ＋ TB 8 | 自有 | 有 | `MappingPreview` |
| 借款利息 | 自有简化版 | 无打分 | 简化 | 只查必填 | 无 | `Mapping` |
| 看账 / 正负数 | 用 `tabular` 的 | 自有 | 固定 9 字段的 struct | 自有 | 有 | `LedgerMappingPreview` |

代价已经付过一次了。这次修汇兑损益时解决的三个问题，另外四个工具原封不动地还在：

1. **科目编码和科目名称混在一个字段里**——存款、借款、看账都还是一个叫"科目编码/名称"的格子；
2. **币种列整列同值时被误当成交易币种**——存款利息的 TB 映射同样有"币种"栏，遇到 SAP 那种整列填本位币的导出会把全表科目当外币；
3. **TB 与 JE 之间没有口径校验**——存款和借款同样是双表工具，两边科目对不对得上无人检查。

统一之后，这类问题一处修好，五个工具同时受益。

## 2. 前提：TB 和 JE 是两种表，角色分开定义

**TB（科目余额表）一行是一个科目在某时点的余额；JE（序时账）一行是一条分录。**
这是两种不同的东西，不能混成一张角色表。本方案里，角色集、必填规则、校验规则
全部按表类型独立定义；共用的只有底层机制（读表、打分、界面渲染）。

按这条线切下来的结果：

| | 内容 |
|---|---|
| **JE 专属角色** | 记账日期、凭证识别字段、凭证类型、摘要、交易币种，以及本位币与原币两套发生额（净额／方向／借方／贷方）。见 §3.2 |
| **TB 专属角色** | 原币币种、币种线索文本、本位币列，以及期初余额、期末余额、本年累计发生额、本期发生额，各有本位币与原币两个口径。见 §3.3 |
| **两表共用角色** | **只有 3 个**：`entity` `accountCode` `accountName` |

这 3 个共用角色**全部是科目身份字段**，没有一个是金额字段，也没有币种。由此得到一条明确的边界：

> **TB 与 JE 的口径对齐，只可能发生在这 3 个字段上；金额与币种永远不跨表比对。**

跨表校验的作用域因此是确定的，不需要逐工具去想"该核哪些字段"。
其中 `entity` 只作分组维度，真正做取值比对的只有 `accountCode` 与 `accountName`。

**关于"辅助信息"槽位**：§3.5 的 TB 六型与 JE 三型末尾都留了一个"辅助信息"可选槽，
但它**不是一个通用角色**——通用 `auxiliary` 已确认删除（见 §3.8）。
这个槽位的角色名按工具而定：存款利息是存款账户与档次，借款利息是 `loanId`，
汇兑损益与看账不启用。槽位统一、语义分开。

**为什么币种不算共用**：两边虽然都叫"币种"，语义并不是一回事——
JE 的币种是**分录级**的"这笔用什么币记账"，TB 的币种是**科目级**的"这个余额是什么币"。
同一个科目在 TB 上可能只有一行 USD，在 JE 上却既有 USD 原始分录、又有本位币记账的调整分录，
两边取值集合天然就不重合。何况 TB 的币种常常根本没有独立列（藏在科目文本里、
整列写的是本位币、或者每个金额列后面跟一个币种列），拿它做跨表交集校验只会制造误报。
**两张表各自定义自己的币种角色，互不参照。**

## 3. 标准角色词汇表

命名沿用汇兑损益现有的一套——它是唯一区分原币与本位币的工具，命名最完整；
而且它刚做过改造、测试护栏最厚，改它风险最大。其余三个工具改名对齐。

### 3.1 金额类角色的命名规则

金额与余额角色的名字是三段拼接：

```
{期间前缀}{币种口径}{方向形态}

期间前缀   （无）凭证发生额 │ opening 期初 │ closing 期末
           period 本期发生 │ ytd 本年累计发生
币种口径   Functional 本位币（默认） │ Foreign 原币（仅汇兑损益启用）
方向形态   Amount 净额 │ Debit 借方 │ Credit 贷方
           （借贷方向单列成 direction，不带口径前缀，见下）

例：opening + Functional + Debit = openingFunctionalDebit（期初本位币借方余额）
```

**`period` 与 `ytd` 是两个不同的东西，必须分开。**
"本期发生"取决于客户导出时选的期间，可能只是一个月或一个季度；
"本年累计发生"才是整个年度期间。**审计要的是后者。**

| 角色 | 含义 | 测算怎么用 |
|---|---|---|
| `ytdFunctionalDebit` / `Credit` | 本年累计发生额 | **首选**。审计期间就是整年，这才是要的口径 |
| `periodFunctionalDebit` / `Credit` | 本期发生额（月／季／年，看导出口径） | 次选。只有它时才用，并提示"这可能只覆盖部分期间，请确认" |

两组都映射上时，一律取 `ytd`。
这条规则同时解决了汇兑损益代码注释里记着的那次踩坑——
SAP 的 MTD（本月）列被误当成 YTD（本年累计）用。有了明确的角色区分，
就不必再靠"借贷两列必须同时映射"这种间接防护。

**方案组**：同一个币种口径下，**借贷方向只有一个角色 `direction`，不分原币/本位币。**
一条分录的借贷方向对两个口径必然相同——不存在原币记借方、本位币记贷方的情况。
所以表里只要有一列"方向"，两个口径共用它。

三种记账形态互斥，任选其一——

| 方案 | 需要映射 | 常见来源 |
|---|---|---|
| A 净额 | `…Amount` | SAP 的"凭证金额""本位币金额"，借正贷负 |
| B 借贷分列 | `…Debit` ＋ `…Credit` | 用友／金蝶的"借方金额""贷方金额" |
| C 净额＋方向 | `…Amount` ＋ `direction` | 金额恒为正、另有一列标"借/贷" |

**原币口径与本位币口径各自独立选方案，互不牵连。**
校验时两组分别判定，所以下面这些组合都合法：

| 原币 | 本位币 | 真实场景 |
|---|---|---|
| A 净额 | A 净额 | SAP：凭证金额 ＋ 本位币金额，都是借正贷负 |
| B 借贷分列 | B 借贷分列 | 国内 ERP：原币借方/贷方 ＋ 本币借方/贷方 |
| C 净额＋方向 | B 借贷分列 | 原币只给一个正数金额，方向列两个口径共用；本位币却是借贷分列的 |
| A 净额 | B 借贷分列 | 混合导出 |

只有汇兑损益启用原币口径；其余工具只有本位币一组，不需要考虑这个组合问题。

### 3.2 JE 角色表（序时账，一行一条分录）

| 标准名 | 中文标签 | 可多列 | 共用 | 说明 |
|---|---|---|---|---|
| `entity` | 公司/核算主体 | 否 | ● | 记账主体，不是交易对手方 |
| `date` | 记账日期 | 否 | | |
| `id` | 凭证识别字段 | 是 | | 多列时按顺序拼成凭证唯一键 |
| `voucherType` | 凭证类型 | 否 | | **仅汇兑损益启用，非必填**。SAP 的 `BLART` 之类 |
| `accountCode` | 科目编码 | 否 | ● | **必填基准**，跨表对齐靠它 |
| `accountName` | 科目名称 | 是 | ● | 允许多列 |
| `summary` | 摘要 | 否 | | |
| `currency` | 交易币种 | 否 | | 分录级：这笔用什么币记账 |
| `functionalAmount` | 本位币净额 | 否 | | 方案组 A |
| `direction` | 借贷方向 | 否 | | 方案组 A，**原币与本位币共用一列** |
| `functionalDebit` | 本位币借方 | 否 | | 方案组 B |
| `functionalCredit` | 本位币贷方 | 否 | | 方案组 B |
| `foreignAmount` / `Debit` / `Credit` | 原币三件套 | 否 | | 仅汇兑损益启用；方向共用上面的 `direction` |

### 3.3 TB 角色表（科目余额表，一行一个科目余额）

| 标准名 | 中文标签 | 可多列 | 共用 | 说明 |
|---|---|---|---|---|
| `entity` | 公司/核算主体 | 否 | ● | |
| `accountCode` | 科目编码 | 否 | ● | **必填基准** |
| `accountName` | 科目名称 | 是 | ● | 允许多列，如"科目名称一级＋二级" |
| `currency` | 原币币种 | 否 | | 科目级：这个余额是什么币。判定见 §3.4 |
| `currencyText` | 币种线索文本 | 否 | | 币种列缺失或漏填时，从这一列的文本里抽币种 |
| `functionalCurrency` | 本位币 | 否 | | 只在**多列币种**时作为消歧结果存在。判定见 §3.4 |
| `openingFunctionalAmount` / `Debit` / `Credit` | 期初本位币余额 | 否 | | 净额 或 借＋贷 |
| `closingFunctionalAmount` / `Debit` / `Credit` | 期末本位币余额 | 否 | | 净额 或 借＋贷 |
| `ytdFunctionalDebit` / `Credit` | 本年累计本位币发生额 | 否 | | **首选口径**。永远只作校验基准，不作测算输入，见 §3.9 |
| `periodFunctionalDebit` / `Credit` | 本期本位币发生额 | 否 | | 次选。可能只覆盖一个月／季，见 §3.1 |
| `openingForeign*` / `closingForeign*` | 原币余额六件套 | 否 | | 仅汇兑损益启用 |

### 3.4 币种列判定规则

TB 里的币种信息分两类，**必须分开处理，混在一起会互相干扰**：

| | 是什么 | 靠什么判 |
|---|---|---|
| **币种代码列** | 写 `USD` / `HKD` / `人民币` 的那列 | 列的取值分布，见下 |
| **金额列的口径标签** | `期初余额(原币)` / `期初餘額-本位幣` / `原币期末余额` | 列名里的"原币／本位币"字样 |

实测 9 份真实 TB：**币种代码列一律叫「币种 / 幣種 / 货币 / Ccy」，没有一份带"本位币""原币"字样**；
带这些字样的全部是金额列。所以列名规则只用于金额列，不用于币种代码列。

#### 币种代码列 → 角色

**取值先归一化**：中文名转代码（美元/美金→`USD`，港币/港元→`HKD`，欧元→`EUR`，日元→`JPY`），
`RMB` 与 `CNY` 视为同一币种；认不出的值（"外币""其他""-"、乱码）**当空处理并告警，不猜**。

**只有一列币种时：**

| 该列取值 | 角色 |
|---|---|
| 完全填满、且只有一个币种 | `functionalCurrency` 本位币列 |
| 其余一切情形（有空白，或出现两种以上） | `currency` 原币列 |

**有多列币种时：**

| 该列取值 | 角色 |
|---|---|
| 币种种类 > 1 | `currency` 原币列 |
| 只有一个币种 | `functionalCurrency` 本位币列 |

即同一条规则：**种类 > 1 就是原币列；填满且唯一才是本位币列。本位币列一定只有一个币种。**

#### 本位币的取值与反判

本位币**默认人民币**，界面上是下拉框，用户可改。已识别到 `functionalCurrency` 列时用该列的值预填；
标题行写了本位币的（实测 5 份都写了，如 `本位币 RMB`、`本位幣 Functional ccy：港元 HKD`）也用来预填。

> **反判规则**：若某列被判为 `functionalCurrency`，但它的取值 **≠ 用户选定的本位币**，
> 该列改判为 `currency` 原币列。
>
> 用途：境外主体的全美元账，币种列填满 `USD` 会被判成本位币列；用户把本位币下拉成人民币之后，
> 判定随之翻转，整表正确识别为外币账。

#### 无币种代码列时

按顺序找替代：

1. **原币金额列**有非零值 → 该科目是外币，但**币种未知**，转 2
2. **科目名称／文本**里的币种词（"美元户""（欧元）""ICBC USD"）→ 取该币种
3. 两者都没有 → 该科目是本位币科目

**纯本币账的判定**（三条全成立）：无币种代码列或其取值全等于本位币；无原币金额列或全空全零；
无任何科目名含币种词。此时**显式提示"未检出外币科目，本表不产生汇兑损益"**——
空结果表分不清是"确实没有外币"还是"识别失败"。

#### 行级取值

币种列被判为 `currency` 之后，逐行取值：

| 币种列 | 原币金额 | 科目名 | 该行判定 |
|---|---|---|---|
| 有外币代码 | — | — | 外币，按币种列 |
| 空 | — | 有币种词 | 外币，按科目名，**标记"币种列漏填"** |
| 空 | 有值 | 无 | 外币但币种未知 → 让用户指定 |
| = 本位币 | 无 | 有币种词 | 余额为零：忽略；有余额：**告警**，人工判 |
| 空 或 = 本位币 | 无 | 无 | 本位币科目 |
| 有外币代码 | — | 与之矛盾 | 按币种列，同时**告警** |

原则：**任一证据指向外币就当外币候选；证据矛盾就告警，不静默取一个。**
"币种列漏填"确有实例——同一张表里一个"美元户"填了 `USD`，另一个"美元户"币种与原币金额双双留空，
只有科目名能救，漏掉就是整笔外币借款不重估。

#### 界面上的「外币」列（2026-08-26）

上面每一条"让用户指定"，此前都没有指定的地方——只能眼睁睁看着测算把科目隔离掉。
科目分类面板（汇兑损益 → 高级设置）现在每个科目一行三列：**科目名 / 科目角色 / 外币**。

后端 `detect_account_currencies` 逐行按 `currency_for` 的同一优先级判一遍，
输出 `accountCurrencyDetails`：

| 字段 | 含义 |
|---|---|
| `detected` | 主币种（该科目出现次数最多的那个） |
| `source` | 依据来自哪里：`币种列` / `科目文本` / `本位币列` |
| `seen` | 该科目出现过的所有币种，进下拉框当候选 |
| `needsConfirmation` | `source` 是 `本位币列` 时为真——**没真识别出来** |

`source = 本位币列` 就是上表最后一行"本位币科目"的判定路径。它有两种截然不同的真相：
科目确实只有本币业务，或者**TB 那列"货币"登记的是主体本位币、科目文本里又没写币种线索**。
两者在数据上无法区分，只能交给人。实测 TB-4800 的 203 个科目里 190 个走的是这条路径，
其中 6 个（过渡银行、应付账款-关联方、其他应付款系列）JE 侧明明有 HKD／JPY 业务，
测算时因此找不到外币余额基础被隔离。

三条设计约定：

- **不预填**。下拉框留空表示"按系统识别的来"，只有用户选过的才进 `accountCurrencies`。
  预填一次就再也分不清"用户确认过 USD"和"系统猜了 USD"，日后改进识别逻辑也推不动已落盘的值——
  主体本位币那一处就是预填踩出来的坑。
- **覆盖按科目编码回退**。TB 与 JE 的科目名拼法不同（TB 是「编码 一级名 二级名」，
  JE 是「编码 科目文本」），界面上勾的是两边并集；只按全名匹配会让指定的币种只对一侧生效。
  `currency_for` 与 `role_for` 用同一套回退。
- **JE 的结论优先于 TB**。JE 逐行读凭证货币，TB 一个科目只有一行；`seen` 取两边并集。

验收：

```bash
cargo test --manifest-path src-tauri/Cargo.toml --test fx_currency_probe -- --ignored --nocapture
cargo test --manifest-path src-tauri/Cargo.toml --test fx_currency_override -- --ignored --nocapture
npx vitest run src/FxAuditPage.test.ts
```

**这一列改变不了 TB 本身没有按币种拆分余额这件事。** 指定币种能让科目进入测算范围，
但如果 TB 只给出该科目一个合计余额（同一应付账款下同时有 HKD 和 JPY），
按币种重估所需的原始数据依然不存在，结果仍是受限测算。

### 3.5 形态型号与整组匹配

角色表说的是"有哪些角色"，形态型号说的是"哪几个角色必须凑齐才算一张能用的表"。

#### TB 六型

| | 期初 | 期末 | 原币余额 | 本年本位币累计 | 本年原币累计 |
|---|---|---|---|---|---|
| **TB1** | 本位币净额 | 本位币净额 | — | 借＋贷 | 借＋贷（可选） |
| **TB2** | 方向＋本位币净额 | 方向＋本位币净额 | — | 借＋贷 | 借＋贷（可选） |
| **TB3** | 本位币借＋贷 | 本位币借＋贷 | — | 借＋贷 | 借＋贷（可选） |
| **TB4** | 本位币净额＋原币净额 | 本位币净额＋原币净额 | ● | 借＋贷 | 借＋贷（可选） |
| **TB5** | 方向＋本位币净额＋原币净额 | 方向＋本位币净额＋原币净额 | ● | 借＋贷 | 借＋贷（可选） |
| **TB6** | 本位币借＋贷、原币借＋贷 | 本位币借＋贷、原币借＋贷 | ● | 借＋贷 | 借＋贷（可选） |

六型共有：`entity`（可选）、`accountCode`（必填）、`accountName`（必填，可多列）、
`currency` 原币列（可选）、`currencyText`（可选）、`functionalCurrency` 本位币列（可选）、辅助信息（可选）。

> **TB1 隐含要求净额带符号。** 它没有方向列，负债科目的期末余额必须是负数，勾稽等式才成立。
> 客户导出绝对值时 TB1 无解——这才是 TB1 与 TB2 的实质区别，不只是"少一列方向"。

#### JE 三型

| | 方向 | 本位币金额 | 原币金额 |
|---|---|---|---|
| **JE1** | ● | 借＋贷 | 借＋贷（可选） |
| **JE2** | ● | 净额 | 净额（可选） |
| **JE3** | — | 净额 | 净额（可选） |

三型共有：`entity`／`date`／`id`／`accountCode`／`accountName`／`summary` 必填，
辅助信息、`voucherType`、`currency` 可选。

> **JE1 的方向列与借贷分列冗余。** 借贷分列本身已表达方向，两者可能矛盾。
> 定为**以借贷分列为准，方向列仅作校验，不一致时告警**。

#### 整组匹配：缺一不可

**一个型在某个槽位里声明的列，必须全部到齐才算命中该型。**
不是逐字段查必填——那样只能报"缺少必要字段"，说不清缺哪个。

| 规则 | 说明 |
|---|---|
| required 槽 | 该型声明的列全部到齐才算命中。缺任意一列 → 不命中 |
| **optional 槽同样整组** | 本年原币累计借＋贷两列，要么都给要么都不给。**只给借方 = 无效**，告警不放行 |
| 未命中任何型 | 取匹配度最高的型，报出**它缺哪几列**——"按 TB5 匹配，缺少『期初方向』" |
| **同时命中多型** | **优先借贷分列**（TB3/TB6、JE1）。借贷能推出净额，反之不行 |

最后一条对应 §3.11 里那个真问题：TB 形态 F、JE 金蝶完整版、JE AX／D365 三个不同 ERP 都是
"借贷两列与净额列同时给"，现行方案组要求二选一会判成不成立。按优先级取一种，不报错。

### 3.6 借贷符号方向判定

同一列"贷方发生额"，实务里有两种写法：写绝对值（语义靠列名承载），或者已经带负号。
两种都常见，**必须判出来**，判错了整表金额全错。

方法对 TB 和 JE 是同一个：**枚举符号假设，用勾稽等式的全表成立率裁决**，只是等式不同。

#### TB：用期末 = 期初 ± 借 ± 贷 反推

待定参数是"贷方性质的列取正还是取负"，记作 s ∈ {+1, −1}：

| 型 | 等式 |
|---|---|
| TB1 | 期末净额 ＝ 期初净额 ＋ 借方 ＋ **s**·贷方 |
| TB2 | 方向还原成符号后同 TB1，另加一个参数判**方向列是否已反映在金额里** |
| TB3 / TB6 | (期末借 ＋ **s**·期末贷) ＝ (期初借 ＋ **s**·期初贷) ＋ 借方发生 ＋ **s**·贷方发生 |
| TB4-6 原币 | 同上，**原币一套单独判**，不沿用本位币的结论 |

#### JE：用凭证内借贷平衡

| 型 | 等式 |
|---|---|
| JE1 | 每张凭证 Σ(借方 ＋ **s**·贷方) ＝ 0 |
| JE2 | 按方向还原符号后 Σ ＝ 0，另判**方向是否已反映在金额里** |
| JE3 | Σ ＝ 0，无待定参数，直接验证是否平衡 |

#### 裁决规则

- **逐行算、全表统计取多数**，不逐行判——个别行数据错不能翻盘
- 舍入容差：绝对 0.01 或相对 1e-6，取大者
- 两个假设都低于阈值（建议 95%）→ **报错"期末与期初＋发生额勾稽不上"**，不硬选一个
- 贷方发生额全为零 → 判不出，但也无影响，标记"未验证"

#### 真实数据上的判别度

拿汇兑损益测试资料里那份 SAP 科目明细（229 行）实跑：

| | 本位币 | 集团货币 |
|---|---|---|
| 期末 ＝ 期初 ＋ 借 **−** 贷 | **229 / 229 ＝ 100%** | **229 / 229 ＝ 100%** |
| 期末 ＝ 期初 ＋ 借 **＋** 贷 | 72 / 229 ＝ 31% | 80 / 229 ＝ 35% |

判别度足够大，不需要复杂的启发式。这份表期初 46 行负数、期末 76 行负数，
**净额确实带符号**，符合 TB1 的隐含要求。

#### 待补：原币的符号验不了

原币余额（TB4-6 的期初/期末）是必填，但**本年原币累计借贷发生额是可选**——
等式缺一项就没法验。二选一：把原币发生额也设为必填，或明确"原币符号沿用本位币结论"
（有风险，实务里两套列可能来自不同导出）。

### 3.7 业务台账：第三张角色表 + 四型整组匹配（2026-08-27）

借款利息的**借款台账**和**利率台账**读的不是 TB/JE，是业务台账——没有科目、
没有借贷方向。它们的角色**不进 TB/JE 标准表**，但已经收进同一个内核，
成为与 `je_roles()` / `tb_roles()` 并列的第三张表 `loan_roles()`，
共用 `alias_score`（最长命中、冲突词一票否决）与 `match_forms`（形态整组匹配）。

收编之前这里是 `loan_interest.rs` 里一份平铺关键词列表，有两个后果：

1. **前后端角色名不是一套**。面板下拉给的是 `closingPrincipal` / `maturityDate` /
   `fixedRate`，引擎识别出来的是 `outstanding` / `endDate` / `rate`，
   自动映射建议落不进格子，用户看到的就是"什么都没识别出来"。
2. **必填校验写死四项**（`loanId`＋期初本金＋期末本金＋利率类型），
   那是变动表的口径。测试集九份台账里六份是"合同本金＋起止日＋期末余额"，
   既没有期初列、利率也直接给数值没有类型列——**必然误报，台账模式点不动测算**。

现在必填由**形态**决定。借款台账四型（`loan_forms()`）：

| 型号 | 起算额（任一即可） | 起始日 | 终点 | 期间发生额 | 利率 | 选填 |
|---|---|---|---|---|---|---|
| A | 本金 ｜ 期初余额 | ✔ | 到期日 | — | ✔ | 利率类型 |
| B | 本金 ｜ 期初余额 | ✔ | 期限 | — | ✔ | 利率类型 |
| C | 期初余额 ｜ 本金 | ✔ | — | 本期新增＋本期归还 | ✔ | 利率类型 |
| D | 期末余额 ｜ 本金 | ✔ | — | 本期新增＋本期归还 | ✔ | 利率类型 |

判定优先级 **A ＞ B ＞ C ＞ D**（数组从弱到强排列，同分时后定义的优先）：
到期日是直接列示的，比拿期限推算准；既有到期日又有四栏时（04 深圳前湾）认 A 型，
四栏转为勾稽校验。九份真实台账全部命中 A 型，基线测试见
`loan_interest.rs` 的 `loan_form_tests`（跑 `cargo test --lib loan_form_tests`）。

**「任一即可」槽是这次为台账新增的槽型**（[`Form::any_of`]）：组内至少一个到位即满足。
起算额三兄弟——本金、期初余额、期末余额——都是"某个时点的占用本金"，
在"全期恒定"的假设下给任意一个都能起算，不必同时给。TB／JE 没有这种槽，两张表一律留空。

**无固定期限借款不纳入**：只有起算额＋起始日＋利率、既无到期日期限也无期间发生额的
那一类（股东借款、关联方拆借、集团资金池、循环贷、永续债）。
当前它落在"最接近 A 型、缺到期日"的未命中态，**不静默当成某一型放行**。
以后要纳入直接往表里加一型即可。

型号与槽位的**唯一定义在 Rust**，随 `loan.inspect` 的 `roles` / `forms` 字段下发；
前端 `src/loanForms.ts` 只实现排序与判定的纯计算（与 `match_forms` 同序），
不再自己抄一份型号定义。

#### 逐行利率口径在预览区确认（2026-08-28）

台账普遍**没有**「利率类型」列，利率列里混着 `3.85`、`0.0365`、`浮动`、`LPR+90BP`
好几种写法。引擎自己猜猜错了没人看得出来——整笔利息就那么算错了。
改成**默认值算给用户看、用户改得动、改完整份回传**：

- 默认值规则在 `src/loanRateTypes.ts`：**台账的「利率类型」列 → 利率列的写法**。
  利率列是纯数值（含 `%`、千分位、括号负数）就是固定；写了浮动字样
  （`浮`／`LPR`／`基准`／`挂钩`／`重定价`／`float`／`variable`）就是浮动；
  两者都不是（空值、「面议」）按固定处理，用户在预览区能看到并改。
- 台账预览区追加两列（`DataTable` 的 `trailingColumns`）：**利率类型**下拉，
  选了浮动才蹦出**上浮(+)/下浮(-) BP** 输入框。默认值随映射实时重算，
  用户的手工改动单独存、叠在默认值上面，两者互不覆盖。
- 整份回传为 `ledgerRateOverrides`，下标 = 台账数据行序。因此**台账的预览行数
  从 8 行放宽到整表**（上限 2000）——只给 8 行的话第 9 行往后没法设置。
  TB／JE 仍是 8 行。
- 浮动的基准取值顺序：**台账的基准利率列 → 利率列的数值 → 内置 LPR 报价表**（`lpr.rs`）。
  台账把 `0.037` 写在利率列、另有一列写「浮动」是最常见的写法（07 星衡），
  这时 `0.037` 就是基准；只写「浮动」两个字、不给数值的才查 LPR。

#### 内置 LPR 报价表（`lpr.rs`）

只存**利率发生调整的日期**（LPR 每月 20 日报价，绝大多数月份与上月持平），
查询取「不晚于定价基准日的最近一次调整」——利率结果与逐月存完全一致，
所以 Sheet 的日期列写的是「利率调整生效日」，不是「那天恰好有报价」。

- **定价基准日 = max(起息日, 报告期初)**。真实重定价日各家合同不同，这是简化，
  所以底稿把这个日期单独列一列、基准利率写成引用它的公式，用户改日期就重算。
- **品种按期限选**：超过 5 年用「5 年期以上」，否则用「1 年期」。
  «5年期以上» 取字面含义，**整 60 个月不算**；比较按月加减而非数天数，
  否则闰年会让同为 60 个月的两笔借款落到不同品种。
- **两条不硬算的边界**：定价基准日早于 2019-08-20（LPR 改革之前，挂的是央行基准
  贷款利率）→ 不套 LPR，标待复核；晚于 `QUOTES_THROUGH` 数据截止日 → 用最后一期
  报价给出完整草稿，同时标待复核并提示补录。
- **数据是默认值不是权威**：表照训练知识录入、未经联网核对，注释与 Sheet 抬头都写明了，
  正式出具前要对照官方公告核。补录新报价时 `QUOTES` 按日期升序插行、
  `QUOTES_THROUGH` 跟着改。

#### 底稿里三列是活公式

导出的 `借款变动与利息测算` 表新增「定价基准日」「LPR品种」「计息本金天数」三列，
并把**基准利率、有效年利率、测算利息**写成公式：

- 基准利率 `=INDEX(LPR报价表!$B:$C, MATCH(定价基准日, LPR报价表!$A:$A, 1), 品种列)/100`
- 有效年利率 `=IF(利率类型="浮动", 基准利率＋加减点/10000, 固定利率)`
- 测算利息 `=计息本金天数 × 有效年利率 ÷ 365`

利息能写成公式，是因为分段计息里利率是常数，
`Σ(本金ᵢ×天数ᵢ) × r ÷ 365` 与逐段累加等价——所以把 `Σ(本金×天数)` 落成一列就够了。
这样**在「LPR报价表」改一个格子，基准利率、有效利率、利息一路重算**。
内置表迟早会过期，过期时用户必须能自己修，而不是拿到一份看不出哪里错了的死数。
`LPR报价表` Sheet 无论有没有行用到 LPR 都会写出来。

界面上的测算期间也一并收敛：**只填资产负债表日**，期间开始由它推出所属年度年初
（`loanReportStart`，与存款利息 `depositReportStart` 同一口径），不再要求填两个日期。

其余角色（`lender`、`loanStatus`、`repaymentMethod`、`benchmarkRate`、`spreadBps`、
`remark` 等）仍可映射，只是不参与形态判定——出表、复核提示、利率推算要用。
历史保存的旧名（`outstanding` / `maturityDate` / `fixedRate` / `benchmark`）
由 `migrate_role_name("loan", …)` 读回来。

但要注意：**借款利息的 TB/JE 映射里那几个角色其实是标准角色的业务别名**，应当归并——

| 借款利息现名 | 归入 | 表 | 理由 |
|---|---|---|---|
| `openingPrincipal` | `openingFunctionalCredit` | TB | 就是期初贷方余额 |
| `closingPrincipal` | `closingFunctionalCredit` | TB | 就是期末贷方余额 |
| `periodDebit` / `periodCredit` | `periodFunctionalDebit` / `Credit` | TB | 同义，改作完整性校验基准（见 §3.9） |

`loanId`（借款标识）**保留为借款利息的专属角色，不并入标准表**。
它原本要并进的通用 `auxiliary` 已经删除（见 §3.8）——语义含糊的通用字段换成
语义明确的专属字段，正是这次收敛想要的方向。

### 3.8 已确认删除的角色

下面这些角色曾经存在于某个工具的映射表里，经逐个核查代码使用点后确认删除。
记录依据，避免以后有人凭直觉再加回来。

| 角色 | 原属 | 判定 | 依据 |
|---|---|---|---|
| `period` 会计期间 | JE | **从未被读取，还制造了假选项** | 全代码只有一处读 `period`，且在只有 TB 会走到的分支里；JE 一律走 `date` 列。更糟的是存款利息前端把它当成"记账日期或会计期间"二选一放行，后端却硬性要求 `date`，只映射会计期间的用户会在点下测算后才撞上"序时账尚未映射记账日期"——**这是现存的一个前后端不一致缺陷**，删掉 `period` 顺带解决 |
| `period` 会计期间 | TB | **有用途，但不该占映射格子** | 唯一用途是从"会计期间"列抠出年份做期间校验。汇兑损益根本没有这个角色，它直接扫描所有表头含"期间"的列就完成了同一件事——改由内核自动探测，不必让用户手工确认 |
| `periodAmount` 本期有符号发生额 | TB（存款利息） | **死字段** | 只在角色表和前端标签里定义，代码中没有任何读取点，连候选打分的加成列表里都没有它 |
| `auxiliary` 辅助核算 | JE ＋ TB | **语义含糊的通用字段，删除** | 三个工具用它做三件不相干的事：汇兑损益只拿它做余额键（重估按科目＋币种算，带不带它结果一样），存款利息拿它认存款档次和银行账户，借款利息拿它当借款标识。一个字段承担三种语义，映射时用户也说不清该填什么。删掉通用角色，需要的工具改用语义明确的专属角色（见下） |
| `clearingId` 清账号 | JE（汇兑损益） | **死字段，已删** | 映射后从未被任何业务逻辑读取，2026-08-22 已随科目拆分一并删除 |

**删掉 `auxiliary` 之后，两个工具需要各自的专属角色顶上：**

| 工具 | 原来靠 `auxiliary` 做的事 | 替代 |
|---|---|---|
| 存款利息 | 用"科目名＋辅助核算"认存款档次；把序时账每笔落到具体银行账户 | 新增专属角色 `bankAccount`（银行账户），语义明确，只在存款利息启用 |
| 借款利息 | 区分同一科目下的不同笔借款 | 沿用它已有的 `loanId`（借款标识），不并入标准表 |
| 汇兑损益 | 只进余额键，重估结果不受影响 | 无需替代。TB 按明细拆行时靠"同键多行自动合并"处理 |

两个专属角色都不进通用标准角色表——**通用表只放五个工具语义一致的字段**，
这正是删掉 `auxiliary` 的目的。

### 3.9 保留但按工具启用的角色

**角色留在标准表里，不等于每个工具都要用它。** 下面三个都是先前被质疑过的，
逐个核查后的结论是：它们各自只在一部分工具里是关键，其余工具不启用即可。

#### `voucherType` 凭证类型 —— 仅汇兑损益，非必填

只有汇兑损益定义了这个角色，存款、借款、看账都没有，而且它不在必填清单里。
它的用途是分类判断：`FX`/`AB` 判自动重估，`DZ`/`ZE` 判结算，`!= AB` 参与"简单结算"判定。
没有它分类会退化，但不阻断测算。

#### TB 本期发生额 —— 永远只作校验基准

**测算主路径全靠序时账**，TB 发生额不参与推算。它的唯一定位是"客户账面记了多少"，
用来跟测算结果比出差异：

| 工具 | 拿它比什么 | 缺失时 |
|---|---|---|
| 汇兑损益 | 账面已入账的汇兑损益 ↔ 测算结果 | 回退到期末累计金额，底稿写明取数口径 |
| 存款利息 | 账面已入账的利息收入 ↔ 测算结果 | 回退到期末余额净额，底稿写明取数口径 |
| 借款利息 | TB 本期贷方／借方 ↔ 序时账汇总出的新增／还款 | 无法校验，该笔借款列为未匹配 |

借款利息这一条是本次修正的重点。**原来的做法是：序时账匹配不上时，改用 TB 发生额
把利息算出来**，行上标"待复核"、`match_basis` 写"未匹配 JE，采用 TB 发生额"。
标记是有的，但结果里仍然多了一个用另一套数据算出来的利息数。

**改为：序时账匹配不上就不出数。** 该笔借款进"未匹配"清单，利息留空，
其余借款照常测算——一笔对不上不该让整张表算不出来，但也绝不能拿替代数据凑一个数出来。
TB 发生额在这里的正当用途是反过来的：**用它校验序时账是否完整**，
序时账汇总出的新增／还款与 TB 发生额不符就报出来。

所以它是**可选角色**，且永远不作测算输入。

汇兑损益的代码注释里还记着一次踩坑——"发生额借、贷方案只有在两列同时映射时才成立，
否则 SAP 的 MTD 列会误替代 YTD"，说明这个字段要成对映射才安全。

另外澄清：**TB 从来就没有原币发生额**。原币口径在 TB 上只有期初与期末余额六件套，
本期发生额只有本位币一个口径。

### 3.10 各工具启用哪些角色

这张表是"工具只声明自己要哪几个角色"的具体样子。
`必填`／`可选`／`—`（不启用）依据现有代码的校验规则整理。

**带 † 的是工具层加严，金标不要求。** 两者都硬阻断，但含义不同：
金标缺失是**这张账表本身不完整**，该找客户重导；工具层缺失只是
**这个测算需要这个输入**，换个工具就不需要（`missing_required` 的 `from_gold` 标记
就是干这个的）。一份没有币种列的序时账在看账里能跑、在汇兑损益里被拦，
不代表这份账有问题。

**序时账（JE）**

| 角色 | 汇兑损益 | 存款利息 | 借款利息 | 看账 / 正负数 |
|---|---|---|---|---|
| `entity` | 可选 | 可选 | — | 可选 |
| `date` | 必填 | 必填 | 必填 | 可选 |
| `id` | 必填 | 可选 | 可选 | 必填 |
| `voucherType` | 可选 | — | — | — |
| `accountCode` | 必填 | 必填 | 必填 | 必填 |
| `accountName` | 可选 | 可选 | 可选 | 可选 |
| `summary` | 可选 | 可选 | 可选 | 可选 |
| `currency` | 必填† | 可选 | 可选 | — |
| 本位币金额四件套 | 必填一组 | 必填一组 | 必填一组 | 必填一组 |
| 原币金额四件套 | 必填一组† | — | — | — |

\* 主体是选填角色：没映射也没填名字时按「默认主体」兜底，不拦（2026-08-29 起，见 §0.3）。

**科目余额表（TB）**

| 角色 | 汇兑损益 | 存款利息 | 借款利息 |
|---|---|---|---|
| `entity` | 可选 | 可选 | 可选 |
| `accountCode` | 必填 | 必填 | 必填 |
| `accountName` | 可选 | 可选 | 可选 |
| `currency` | 必填†** | 可选 | 可选 |
| `currencyText` | 可选** | — | — |
| `functionalCurrency` | 可选 | — | — |
| 期初本位币余额 | 必填一组 | 必填一组*** | 必填 |
| 期末本位币余额 | 必填一组 | 必填一组 | 必填 |
| 本期本位币发生额 | 可选（校验用） | 可选（校验用） | 可选（校验用） |
| 原币余额六件套 | 可选 | — | — |

\** 交易币种列与币种线索文本二选一，至少要能认出外币科目。
\*** 上传了序时账时可以不给期初，由期末倒推。

### 3.11 真实样例形态清单

角色表定稿之前，要用真实样例验证它够不够用。以下是在本机全盘搜索
"科目余额／序时账／余额表／TB／gl_journal" 之后，扫过表头结构的样例归纳。

**两条已定的原则，可以砍掉一批特殊设计：**

1. **缺必要字段的导出，直接报错提示用户补，不为它做特殊分支。**
   例如只有 MTD／YTD 累计、没有期初余额的报表型导出，就提示"缺少期初余额"让用户重导，
   不去设计一套"累计口径"的平行模型。
2. **币种识别不需要"跟随列""这列币种属于前面那列金额"之类的概念。**
   币种代码列的角色由取值分布决定（§3.4），哪怕一份表里有四个都叫 Currency 的列也能筛出来；
   金额列的原币／本位币口径由列名决定。两条互不干扰。

#### 科目余额表（TB）已确认形态

| 形态 | 来源 | 科目怎么给 | 余额口径 | 币种 |
|---|---|---|---|---|
| **A · SAP 科目明细** | 汇兑损益测试资料 | 名称一级＋二级＋科目代码，三列 | 期初／借方／贷方／期末，本位币与集团货币各一套。**完整命中 TB1**；集团货币四件套与"绝对差异"两列属噪声，须排除 | "货币"列 229 行全填 `USD`（该主体本位币），按 §3.4 判为本位币列；账户原币藏在"文本"列（`银行存款-建行RMB3250`、`建行HKD5050`） |
| **B · SAP 报表三合一** | 汇兑损益测试资料 | GL Account＋GL Description | 只有 MTD 与 YTD 累计 | 四个列都叫 Currency |
| **C · 用友标准余额表** | 汇兑损益测试资料 | 科目编码＋科目名称 | 期初／本期发生／期末，全借贷分列 | 无币种列 |
| **D · 金蝶双层表头** | 审计底稿材料 | 科目编码＋科目名称＋**核算维度编码／名称** | 期初／本期发生／**本年累计**／期末，每组借贷分列，另有"方向"列 | 无币种列 |
| **E · Oracle 全年 TB** | 集团税务资料 | Account＋Desc 与 SL Account＋Desc **两套科目并存**，由跨表对齐决定用哪套 | 期初期末给**净额**，本期发生给**借贷分列** | 藏在科目名称里，反斜杠分隔（`…\中国工商银行\CNY 68078`） |
| **F · Oracle 分月 TBD** | 集团税务资料 | 同上，另有 Accounting Flexfield 完整组合键 | 期初／借方／贷方／**净发生额**／期末，**借贷与净额三列同时给** | 同 E |

#### 序时账（JE）已确认形态

| 形态 | 来源 | 凭证键 | 金额 | 备注 |
|---|---|---|---|---|
| SAP FI 明细 | 汇兑损益测试资料 | 凭证号码 | 凭证金额（原币净额）＋本位币金额＋集团货币金额 | 已验证 |
| SAP YTD 导出 | 汇兑损益测试资料 | Document Number | Document／Company Code／Group Currency Value 三套净额 | 大文件，走样本识别 |
| **Oracle 集团模板** | 集团税务资料，约 20 家公司同模板 | 无单一凭证号，要 **Batch Name＋JE Name** 组合 | Debits／Credits（本位币借贷分列）＋ Enter Debits／Enter Credits（原币借贷分列） | 两个币种列：`Currency` 是本位币、`Enter Currency` 是交易币种 |
| **金蝶序时账** | 审计底稿材料 | **凭证字＋凭证号** 两列组合 | 原币金额（净额）＋借方金额／贷方金额（本位币借贷分列） | 币别是中文"人民币"；另有审核状态／作废状态列 |
| **金蝶完整版** | 审计底稿材料 | **凭证字号合成一列**（"转账 - 2"），另有**分录号** | 原币金额＋借方／贷方，**三列同时给**，且冲回分录金额为负 | 与前一种金蝶导出的凭证键结构不同 |
| **AX／D365 凭证行分析** | 微信收到的客户材料 | 凭证号 | 借方／贷方金额（本位币）＋"货币借方／贷方金额"（原币）＋"金额（总和）"净额，**三列同时给** | 摘要叫"文本"；**因 65536 行上限被切成多个 sheet，且分片之间列数不同** |
| 分录导入模板 | 集团税务资料 | 无凭证号，是待导入的分录 | DR Amount／CR Amount | **表头下面还有一行字段说明**（"Company*
Text"） |

#### 这些样例打出来的真问题

| 问题 | 出自 | 现在会怎样 |
|---|---|---|
| **借贷两列与净额列同时存在** | TB 形态 F、JE 金蝶完整版、JE AX／D365——**三个不同 ERP 都这样，是普遍模式不是个例** | 现行方案组要求"净额与借贷分列二选一"，三列齐全反而判成**方案不成立**。必须改：三者同时存在时按优先级取一种并说明，不报错 |
| **"本年累计"与"本期发生"必须分开** | TB 形态 D（两组都有）、形态 B（MTD 与 YTD） | 已定：新增 `ytd` 前缀，审计一律取本年累计，见 §3.1 |
| **余额方向列** | TB 形态 D 的"方向"列（借／贷） | 现有 `Direction` 角色是给发生额用的，余额方向是另一回事 |
| **凭证键需要多列组合** | JE Oracle 模板、金蝶 | 现有 `id` 支持多列，**这条已经满足**，但要确认自动映射能选中两列 |
| **表头下有一行字段说明** | JE 分录导入模板 | 表头探测很可能把说明行当第一条数据 |
| **同一文件不同 sheet 表头措辞不同** | TB 形态 E（`Begin Balance` vs `Beginning Balance`） | 别名表要同时覆盖两种写法 |
| **中文币别** | JE 金蝶（"人民币"） | 归一化已支持，需回归验证 |
| ~~作废凭证要过滤~~ | JE 金蝶（审核状态／作废状态列） | **已定：不过滤。作废凭证也是凭证，审计要看全量**。这些状态列不进角色表 |
| **科目编码格式要归一化** | JE 金蝶（`2241.02`）、TB 形态 F 的 Flexfield、科目名称里的 `\` 层级分隔 | **已定：要做归一化**。跨表对齐前统一去掉分隔符与前后空白再比对，否则 `2241.02` 与 `224102` 会判成对不上 |

#### 抽查结论：集团模板高度一致

对 22 份 Oracle 集团序时账（覆盖 20+ 家公司、中日韩马印港法墨沙特等 8 个国家地区）
逐份比对表头，**只有 2 种签名，差别仅在多一列 `Description`，其余 37 列完全相同**。

同样地，Oracle 的分月 TBD 与全年 TBD 两份表头完全一致。

**含义**：集团口径的样例只需针对模板做一次映射规则，就能覆盖所有子公司；
真正的形态风险来自不同 ERP（金蝶、用友、AX／D365、SAP、Oracle）之间，
以及同一 ERP 的不同导出方式（金蝶就有"凭证字＋凭证号"两列和"凭证字号"一列两种）。

#### 还需要补的样例类型

多期间横向展开（1–12 月各一组余额）、合并层含抵销分录列、浪潮／SAP B1 导出、
带辅助核算的明细余额表。**这份清单是开放的**，角色表定稿的标志是
"手上所有真实样例都能只靠这张表映射出来"。

### 3.12 字段库

**字段库不是一张别名表，是五样东西：**

| 组成 | 作用 |
|---|---|
| 角色清单 | JE 一份、TB 一份，见 §3.2／3.3 |
| **别名库** | 每个角色在实务中的所有写法，中英文都要 |
| **冲突词库** | 防止短别名把别的列吃掉——这是现有实现最大的坑 |
| 数据形态判定 | 列名天生分不出时看数据 |
| 歧义清单 | 判不了就让用户确认，不猜 |

#### 覆盖率验证

拿 §3.11 那 12 种真实形态的**全部 129 个列名**，逐列标注"应该落到哪个角色"，
再跑现有别名库和补充后的字段库：

| | 正确 | 认不出 | 映射错 | 噪声命中 |
|---|---|---|---|---|
| 现状（`fx.rs` 里的别名库） | **76 / 129 ＝ 59%** | 39 | 12 | 2 |
| 字段库 v2（补别名＋冲突词） | **127 / 129 ＝ 98%** | 0 | 2 | 0 |

剩下那 2 条是同一个问题——`货币` 与 `Currency` 光看列名分不出是本位币还是原币，
而这正是 §3.4 该管的：填满且唯一才是本位币列，其余都是原币列。
按 §3.4 判定后实际有效准确率是 129／129。

**验证脚本可复用**：每收到一份新形态的样例，把它的列名和标准答案加进去跑一遍，
准确率掉了就说明别名或冲突词要补。建议固化成回归测试。

#### 现有实现暴露的两个机制缺陷

补别名之前，先得修这两个——它们让"补别名"这件事本身失效：

| 缺陷 | 现象 | 修法 |
|---|---|---|
| 短别名把长列名吃掉 | `functionalCurrency` 的别名"本位币"吃掉了 `期初金额-本位币`、`借方金额-本位币`、`期末金额-本位币`——SAP 那份 TB 的**所有金额列**都被判成"本位币字段" | 给它加冲突词 `金额／余额／balance／amount／发生／差异` |
| 同上 | `accountCode` 的别名 `account` 吃掉了 `Account Desc`、`Accounting Flexfield` | 加冲突词 `desc／description／名称／文本／flexfield／segment` |

匹配打分也要改：现在是**找到第一个匹配的别名就停**，应该改成**遍历所有别名取最长匹配**——
否则 `借方金额` 会被 `金额` 抢在 `借方金额` 之前命中。

#### 别名库（TB）

| 角色 | 实务写法 |
|---|---|
| `entity` | 公司代码、公司名称、核算主体、主体、`Company`、`Company Code`、`Break Segment`、`Entity` |
| `accountCode` | 科目编码、科目代码、科目号、总账科目、会计科目、`Account`、`GL Account`、`Account Code`、`SL Account` |
| `accountName` | 科目名称、科目名称一级、科目名称二级、科目描述、账户名称、`Account Desc`、`Account Description`、`GL Description`、`SL Account Desc` |
| `currency` | 币种、货币、原币币种、交易币种、`Currency` |
| `currencyText` | 文本、科目文本、账户文本、说明、备注、描述 |
| `functionalCurrency` | 本位币、功能货币、货币、`Currency`、`Functional Currency`（仅多列币种时；单列一律先按 §3.4 判） |
| `openingFunctional*` | 期初余额、期初金额、年初金额、期初本位币余额、`Begin Balance`、`Beginning Balance` |
| `closingFunctional*` | 期末余额、期末金额、年末金额、`End Balance`、`Ending Balance`、`YTD Act`、`YTD Actual` |
| `periodFunctionalDebit` | 本期发生借方、本期借方发生额、借方金额、本期借方、`Period Dr`、`Period Debit` |
| `periodFunctionalCredit` | 本期发生贷方、本期贷方发生额、贷方金额、本期贷方、`Period Cr`、`Period Credit` |
| `periodFunctionalAmount` | 本期净发生、`Period Activity`、`MTD` |
| `ytdFunctionalDebit` / `Credit` | 本年累计借方／贷方、`YTD Debit`／`YTD Credit` |

#### 别名库（JE）

| 角色 | 实务写法 |
|---|---|
| `id` | 凭证号、凭证号数、凭证编号、凭证字、凭证字号、`Voucher`、`Document Number`、`Batch Name`、`JE Name`、`BELNR` |
| `date` | 日期、记账日期、记帐日期、过账日期、凭证日期、`GL Date`、`Posting Date`、`BUDAT` |
| `voucherType` | 凭证类型、凭证类别、`Category`、`Document Type`、`BLART` |
| `accountCode` | 科目编码、科目代码、会计科目、总账科目、账户、`Account Code`、`G/L Account`、`SAKNR` |
| `accountName` | 科目名称、科目文本、科目描述、科目全名、`Child Description`、`Account Desc` |
| `summary` | 摘要、文本、`Entry Item`、`Line Description`、`Text`、`SGTXT` |
| `currency` | 币种、币别、货币代码、交易币种、凭证货币、`Enter Currency`、`Document Currency Key`、`WAERS` |
| `functionalCurrency` | 本位币、`Ledger Currency` |
| `direction` | 方向、借贷方向、借贷、`Dr/Cr` |
| `functionalAmount` | 本位币金额、本币金额、借正贷负、金额、`Company Code Currency Value` |
| `functionalDebit` / `Credit` | 本位币借方／贷方、借方、贷方、借方金额、贷方金额、`Debits`／`Credits` |
| `foreignAmount` | 原币金额、外币金额、凭证金额、原币、`Document Currency Value` |
| `foreignDebit` / `Credit` | 原币借方／贷方、外币借方／贷方、货币借方金额、货币贷方金额、`Enter Debits`／`Enter Credits` |

#### 冲突词库

| 角色 | 冲突词 | 挡住什么 |
|---|---|---|
| `functionalCurrency`、`currency`（TB） | 金额、余额、balance、amount、发生、差异 | `期初金额-本位币` 之类的金额列 |
| `accountCode` | desc、description、名称、文本、flexfield、segment | `Account Desc`、`Accounting Flexfield` |
| `accountName` | flexfield、segment、code | `Accounting Flexfield`、`Account Code` |
| `currency`、`functionalCurrency`（JE） | 金额、amount、value | `Document Currency Value` |

#### 数据形态判定（列名不够用时）

| 判什么 | 怎么判 |
|---|---|
| 币种列是本位币还是原币 | 归一化取值后：种类 > 1 即原币列；填满且唯一才是本位币列。完整规则见 §3.4 |
| 科目编码 vs 科目名称 | 编码列取值形如字母数字编码；名称列是可读文本 |
| 哪一套科目跟 JE 对得上 | Oracle 那种 `Account` 与 `SL Account` 并存的表，**由跨表对齐用实际取值决定用哪套，两套都对得上就都用** |
| 日期列 | 可解析为日期的比例 |
| 金额列 | 数值占比 |

## 4. 校验也按表类型分开

### 4.0 先立一条通则：对不上就报出来，不拿别的数据凑

审计工具出的每一个数，都要说得清是从哪来的。**数据对不上时，正确的动作是把它报出来，
而不是换一套数据源算出一个数**——哪怕旁边标了"待复核"，那也是一个会被读进底稿的数字。

判断一个回退能不能接受，看它换的是"取数途径"还是"结论依据"：

| | 例子 | 判定 |
|---|---|---|
| **可接受**：同一业务含义的两种取数途径，且底稿写明用了哪种 | 损益科目的本年累计金额，既可以取本期发生额，也可以取期末余额 | 保留 |
| **不可接受**：换一套数据源算出结果，结论的依据变了 | 借款利息在序时账匹配不上时改用 TB 发生额算出利息 | **改掉**，见 §3.9 |
| **正确示范** | 存款利息把序时账分录落到银行账户：能唯一确定就落，落不准且候选多于一个就跳过留待复核，不猜 | 照此办理 |

按这条通则，本次要改的是借款利息那处；存款利息落账那处本来就是对的，可以当模板。

---

校验规则跟角色一样，不混在一起。三类各自独立：

### JE 专属校验
- 凭证唯一键（主体＋日期＋凭证识别字段）无空值
- 无完全重复的明细行
- 金额方案组成立：本位币一组；启用原币口径时，原币也要一组
- 数据期间与报告期一致

### TB 专属校验
- 期初、期末至少各有一组余额（净额 或 借＋贷）
- 同一余额键（主体＋科目＋币种）的多行**自动合并**，不再报错
- 余额列的有效数值比例达标
- 币种来源可确定：有交易币种列，或能从科目名称／币种线索文本里认出外币科目
- 与 JE 同时使用时，期初与期末要有一组同口径余额（原币对原币，或本位币对本位币）

### 跨表校验（仅在两表都上传时）
- 作用域**只有 §2 那 3 个共用角色**，实际核 `accountCode` 与 `accountName`
- 按实际取值比对重合度；对不上则在两张表的低基数列之间自动搜索能对上的列并改映射
- 编码与名称至少一个对得上才放行，两个都对不上才拦截

## 5. 各工具改名清单

| 工具 | 需改角色数 | 明细 |
|---|---|---|
| 汇兑损益 | 0 | 作为命名基准，不动 |
| 存款利息 | 约 15 | JE：`voucherId`→`id`，`account`→拆成 `accountCode`＋`accountName`，`amount`/`direction`/`debit`/`credit` 加 `functional` 前缀。TB：`account`→拆分，`openingBalance`→`openingFunctionalAmount`，六个借贷余额角色加 `Functional`；`period` 与 `periodAmount` 直接删除（见 §3.8） |
| 借款利息 | 约 12 | TB / JE 侧同上；`openingPrincipal`／`closingPrincipal` 按 §3.7 归并；`loanId` 保留为专属角色；序时账匹配不上时改为不出数（§4.0）；台账与利率台账不动 |
| 看账 / 正负数 | 约 6 | 只有 JE：`account`→拆成 `accountCode`＋`accountName`；四个金额角色加 `functional` 前缀；`id` 保持 |

## 6. 三层架构

### 第一层：标准角色词汇表（纯数据）

**两份清单，JE 一份、TB 一份**，共用角色在两份里各出现一次（不抽公共段，
避免归属不清）。每个角色记：标准名、中文标签、别名、冲突词、能否多列、
是否必填、属于哪个方案组、属于哪个币种口径。

### 第二层：后端映射内核（新增模块）

**入口按表类型分**：`inspect(source, Je)` / `inspect(source, Tb)`，
各自取自己的角色集和校验集，互不引用。工具只声明"我这张 JE 要哪几个角色、
我这张 TB 要哪几个角色"。

**共用的是机制，不是角色**：

- 读表与表头探测（含大文件轻量路径、Excel 日期格式还原、两层表头合并）
- 按别名＋数据形态给每列打分，生成候选与建议映射
- 映射列存在性、方案组互斥、必填这三类通用校验的执行框架
- LLM 复核提示词**从角色清单自动生成**——现在每套一段手写提示词，
  是角色改名后最容易忘记同步的地方

**跨表对齐是独立的一块**，不属于 JE 也不属于 TB，只在两表都存在时运行，
作用域限定在 3 个共用角色（已在汇兑损益里实现，本就是通用逻辑）。

**数据年度探测收进内核**：TB 的年度不再依赖 `period` 角色，改为扫描所有表头
含"期间"的列自动推断（汇兑损益的现成做法），用户不必为此手工映射一列。

### 第三层：前端统一映射面板

一个组件，但**JE 面板与 TB 面板各渲染一次**，下拉里只出现该表类型的角色，
不会让用户在序时账里看到"期末余额借方"这种选项。组件负责：表头下拉选角色、
多列角色、方案组互斥置灰、必填提示、LLM 复核、口径核对结果展示。

角色的中文标签由后端随识别结果一起下发，前端不再各自硬编码一份
（现在改一个标签要改两处，容易漂移）。

## 7. 迁移步骤

每步独立可验收，随时可以停在任一步。

| 步骤 | 内容 | 为什么是这个顺序 |
|---|---|---|
| 0 | **穷举 TB 形态**，用真实样例验证角色表 | 见 §3.11。TB 形态比序时账散得多，角色表没经过真实样例检验就定稿，后面每碰到一种新形态都要返工 |
| 1 | 定 JE 与 TB 两份角色词汇表 | 命名先对齐。就是本文 §3，但要等第 0 步验证过 |
| 2 | 抽出后端内核，**汇兑损益**先切 | 它最完整、两种表都有，测试护栏最厚，切它能最快暴露内核抽象是否够用 |
| 3 | **存款利息**切过来 | 已经在复用汇兑损益的表结构，改动最小；顺带白拿科目拆分、币种新规则、跨表对齐 |
| 4 | **借款利息**切过来 | 要换掉自有读表，并把 TB/JE 角色归并到标准名；测试最薄，**动之前先补测试** |
| 5 | **看账＋正负数**切过来 | 只有 JE，不涉及 TB 那半边；但在 `tabular.rs` 里，要把固定字段的 struct 改成通用映射，且等并行修改停手 |
| 6 | 前端四套界面合并 | 后端契约稳定后再动 UI 最省事 |

## 8. 风险与护栏

**角色改名会让历史任务参数失效。**
任务历史里存着旧角色名的映射参数，改名后读不出来。
处理办法是加兼容层：读参数时把旧名翻译成新名。
汇兑损益这次的 `account` → `accountCode`＋`accountName` 已经验证过这个手法可行
（见 `fx.rs` 的 `account_columns`），照搬即可。

**借款利息的测试太薄。**
截至 2026-08-22，四个模块的测试数量差距明显，借款利息是唯一需要"先补护栏再动"的。
现场核对：

```bash
for f in fx deposit_interest loan_interest tabular; do echo "$f: $(grep -c '#\[test\]' src-tauri/src/$f.rs)"; done
```

**`tabular.rs` 存在并行修改。**
第 4 步动它之前必须确认没有其他会话正在改，否则冲突难解。

**每步都要跑各自的验收矩阵。**
汇兑损益没有 `*_PARITY.md`（它是新工具，不是迁移工具），
但有真实客户样例的 `#[ignore]` 测试，改动后必须显式跑：

```bash
cargo test --manifest-path src-tauri/Cargo.toml fx::tests -- --ignored
```

看账那部分要同步更新 `KANZHANG_PARITY.md`。

## 9. 怎么算做完了

- JE 与 TB 各有一份角色清单，五个工具都从这两份里取，没有任何工具自带私有角色表（业务台账角色除外）
- 序时账的映射界面里不会出现余额类角色，科目余额表的界面里不会出现凭证类角色
- 候选打分、必填校验、方案组校验、LLM 提示词各只有一份实现
- 跨表对齐是独立一块，作用域限定在 3 个共用角色，不含币种
- 每个角色都有实际读取点；新增角色前先确认它会被业务逻辑用到
- 中文标签只在一处定义
- 新增一个需要读账表的工具时，只需声明角色，不需要复制任何映射代码
- 全量测试通过，四个真实客户样例测试通过

## 10. 2026-08-28 公共复核生命周期与业务覆盖口径

- `LedgerReviewAll` 以数据源标识和复核代次隔离异步结果；切换文件、重新识别或组件卸载后，旧请求不得回写到新映射。存款、汇兑和 FA 直接使用同一组件与同一公共 TB/JE 复核入口，不建立工具专属提示词。
- 存款科目角色拆为“自动分类结果”和“用户覆盖”两层。自动排除不等同于用户排除；父科目人工分类可以向未人工覆盖的末级科目继承，末级明确排除优先。
- 公共一致性测试使用合成 TB/JE 核对汇兑、存款、看账和 FA 的 Sheet、标题行、映射、标准净额与符号结论，避免依赖客户文件。看账与 FA 的正负数 Net=0 状态来自同一个公共函数。
- 借款台账已有执行利率时，默认类型为固定利率并保留该执行利率；只有用户明确改为浮动利率，才清空固定执行利率，改按公共识别的基准利率加减点计算。没有独立基准时使用对应期间 LPR 回退并提示复核，不得把原固定执行利率当作浮动基准。

## 11. 2026-08-30 收口：各工具不再自持一份规则

一次系统扫描发现，五个账表工具虽然都"接了引擎"，仍各自留着一批**本地实现的同类规则**——
同一件事两三份写法，改一处不改另一处就产生口径分歧。这一轮把它们收进 `ledger_mapping.rs`。

工具侧只保留**本工具特有的词汇**（汇兑损益的外币识别、借款的台账角色这类），
其余判定一律由引擎出结论、工具套用结论。

### 收进引擎的规则

| 规则 | 原先在哪 | 现在 |
|---|---|---|
| 金额文本解析 | 存款、借款、FA 账表各一份 | `parse_amount_lenient`：千分位、货币符号、括号负数、尾部负号、CR/DR 与借贷后缀统一一份口径。百分号只剥符号不除以 100——利率列要不要换算由调用方决定 |
| 汇总／小计行词表 | 汇兑一份、引擎一份 | `is_rollup_label`：兼认繁体、`本期合计`／`累计`、带前缀的小计行与行尾冒号 |
| 工作表挑选打分 | 汇兑一份、看账一份 | `sheet_score` + `is_auxiliary_sheet_name`：行数按对数加权，25 万行的正表压得过 384 行的透视副本 |
| 表头行打分 | 汇兑一份、看账一份 | `header_row_score` + `header_semantic_hits`：关键词命中封顶 8 个，不让词表信号吞掉其余四项判据 |
| 角色命中判定 | 汇兑自带一套别名判定 | 一律以 `alias_score` 的返回为准，工具的列数据画像只作**乘数**叠加，不替代判定 |
| 合并科目列兜底 | 汇兑一份、存款一份 | `plan_combined_account_fill`：两级挑选（先挑没人占的列，再从辅助核算、币种线索这类弱角色手里抢），引擎只出结论，套回映射表的十来行留在工具侧 |
| 科目编码归一 | 汇兑的匹配键没做前导零归一 | 匹配键统一走 `normalize_account_code`，序时账补零科目与余额表不补零科目能对上账 |
| 必填校验 | 前端拦、Rust 侧不拦 | `missing_required` 前移到 Rust 入口，缺映射直接报 `MAPPING_INCOMPLETE` 并点名缺哪个角色 |
| 中文角色标签 | 五个页面各抄一份 | 识别结果下发 `roles: [{name, label}]`，前端 `resolveRoleLabels` 后端优先、本地兜底 |

### 一个跨工具的坑：先填充还是先剔除噪声行

序时账常用合并单元格表示"与上一行同一张凭证"，读进来只有首行有值，所以要向下补齐。
但合计行、表尾手工草稿、游离数字行**本来就没有身份**，照常补齐会让它们继承上一行的
科目和凭证号，摇身变成真分录混进发生额——借款利息实测踩过（合计行计入本金变动），
汇兑损益的生产路径同样存在。

修法写进引擎：`forward_fill_columns_skipping`。噪声行**既不接收也不传播**填充值
（它可能把"合计"写在摘要这类可填充列里，传播出去会把下一个空行也染成合计），
且**行留在原位**——报表按源表行号溯源，不能删行。

### 刻意没有统一的部分

- **外币识别**（汇兑损益）：只有这个工具需要区分原币与本位币，属工具特有词汇，留在工具层显式声明。
- **存款的必填豁免**：引擎的六型形态要求"期初＋期末＋本年累计借贷成对到齐"，比前端严得多，
  照搬会误拦三套真实样例（SAP 的 TB 没有年初列、净额式余额表没有发生额列、序时账没有科目名称列）。
  存款把豁免**预填进已映射集合**再交给引擎判定——判定规则仍是引擎那一份，豁免是工具的显式声明。
- **业务台账角色**（借款）：第三张角色表，与 TB/JE 并列，本来就不共用。

### 验收

```bash
cargo test --manifest-path src-tauri/Cargo.toml --lib
npx vitest run
```

真实样例（调查用，需显式指定目录）：

```bash
LEDGER_SAMPLES=<样例目录> cargo test --manifest-path src-tauri/Cargo.toml --lib 真实样例的三条核对 -- --ignored --nocapture
```

本机四张余额表的结论：TB-3300、TB-4800 三条全通过；`Oct+BS+PL+TB.xlsx` 恒等式差
−63,855,852.10，对应 43 个认不出会计要素的科目被单列（文档写明的设计，不猜类别凑平）；
`科目余额表.xls` 年末差 30,820.00，三个类别合计正好等于该差额、没有被排除的科目，
**是该样例文件年末本身不平**，需要人工确认这份样例的口径，不是识别或计算问题。
