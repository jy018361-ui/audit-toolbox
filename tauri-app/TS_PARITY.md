# TS 管理迁移一致性矩阵

## 2026-09-04 · XLS 输入统一

已接入共享 XLS 内容识别/文本读取或模板准备路径。保留本工具的字段映射和业务规则；格式、模板依赖、已覆盖与未覆盖范围及回归命令见 [XLS 输入兼容范围](XLS_INPUT_COMPATIBILITY.md)。不将文件格式兼容表述为旧版全部业务行为等价。

基线源代码：`tools/TS/cop123213y.py`。目标实现：`src/TsManagerParityPage.tsx` 与 `src-tauri/src/tabular.rs`。生产数据处理路径为 Rust Polars，不调用 Python Polars。

| 能力 | 旧版语义 | Tauri/Rust 状态 | 验收证据 |
|---|---|---|---|
| 文件类型 | XLSX/XLSM/XLS/CSV/TXT | 已实现；另兼容 TSV/Parquet 内核输入 | Rust 单测、`load_table` |
| Sheet | 多 Sheet 时选择，单 Sheet 默认首张 | 已实现，读取后列出所有 Sheet | `ts.inspect` |
| 标题行 | 1–50，默认 1 | 已实现并校验越界 | 页面 + `HEADER_ROW_INVALID` |
| CSV 编码/分隔符 | UTF-8/GB18030/UTF-16；逗号、Tab、分号、竖线 | 已实现自动探测 | `load_text` |
| 默认筛选 | Department Name = ASU Delivery Center ZZ-WP | 已实现，并保留旧版 27 项部门快捷值 | 前端单测 |
| 组合筛选 | **旧版主界面只有一行筛选（一个字段一个值），无增删按钮**；多条件代码只存在于已停用入口 | 已实现多行筛选：同字段多值 OR、不同字段 AND、支持空白，可增删筛选行并读取 distinct 值 | Rust/前端单测。**这是能力增强，不是与旧版持平** |
| 默认字段 | COE Manager 等 9 个字段 | 已按原顺序实现，缺失时回退前 4 列 | `ts_defaults` |
| by经理 | Employee Name 固定第二顺位 | 已实现 | `ts_defaults` |
| by项目 | Engagement Name 第一、Employee Name 第二 | 已实现 | `ts_defaults` |
| 列字段 | Transaction Cycle Date，回退 Month | 已实现 | `ts_defaults` |
| 值字段 | Hours，回退首列 | 已实现 | `ts_defaults` |
| 聚合 | sum；内核兼容 count | 已实现 Rust Polars group-by/pivot | Rust 单测 |
| 宽表保护 | 透视列去重值最多 180 | 已实现同一上限及用户提示 | Rust 单测 |
| 默认导出 | by经理 + by项目 两张 Sheet | 已实现；页面只暴露原版正式入口 | Rust 导出单测 |
| Excel 样式 | 表头加粗、数值格式、冻结 J2、筛选、动态列宽、中段列折叠 | 已实现 | `write_pivot_sheet` |
| 原始明细 | 可选输出同筛选口径 `_data.csv`，UTF-8 BOM | 已实现 | `write_csv_table` |
| 默认文件名 | `Timesheet_Default_Dual_时间戳.xlsx` | 已实现 | `output_path` |
| 缓存 | 源文件/mtime/大小/Sheet/标题行隔离的 Parquet | 已实现稳定 Rust Polars Parquet，损坏自动重建、原子替换 | Rust 缓存单测 |
| UNC 路径 | 支持网络共享，缓存后避免重复回源 | 原路径直接读取，缓存键保留 canonical/UNC 身份；缓存命中不再回源读取数据 | `load_ts_cached` |
| 进度/取消 | 后台任务阶段进度、用户取消 | 已实现统一任务事件与协作取消 | `ts.export` job |
| 选文件起始目录 | 对话框默认打开 FY27 网络共享目录，选过一次后记住上次文件所在目录 | 已实现：上次目录（sessionStorage）→ FY27 UNC 常量 → 系统默认；不可达时静默退回，不报错 | 前端单测 `pickerStartDirectory` / `parentDirectory`、Rust `dialog_start_directory` |
| 选完自动读取 | 选中文件立即读取并建缓存（`_confirm_selected_file`），不需要再点按钮 | 已实现：选中后自动触发 `ts.inspect` 任务；「加载文件」按钮保留供改 Sheet/标题行后重读 | `chooseInput` → `inspect(selected)` |
| 页面缓存 | 工具间切换不丢输入 | 已实现窗口生命周期内草稿缓存 | 模块级 draft |
| 输出安全 | 旧版直接写目标 | 改进为临时文件完成后原子替换，取消/失败不留下半文件 | `partial_path`/`replace_file` |

## 仍需真实数据验收

- 超大真实 Timesheet（尤其 UNC 路径）的首次读取、二次缓存命中耗时和峰值内存。
- 内网环境下文件对话框确实开在 FY27 共享目录、以及断网时退回系统默认目录的实际观感（当前依据是 rfd/Windows Shell 解析失败即跳过设定目录的代码路径，未在真实断网机器上实测）。
- 真实日期单元格、混合字符串日期在导出列头中的显示是否与项目组当前模板完全一致。
- 超过 Excel 工作表行列限制、被占用输出文件和无写权限目录的用户提示。
- 用户正式样例的 by经理/by项目语义对比：字段顺序、分组值、每月工时与总行数。

以上项目需要脱敏真实样例或目标网络环境，不影响已完成的代码级处理语义迁移，但在真实样例验收前不应宣告压力指标达标。


---

## 2026-08-05 迁移差异审计与修复

本轮对九个工具做了逐项"旧版 vs 新版"审计，结论按"功能没了 / 结果可能不一样 / 少了提醒 / 本来就该丢掉 / 新版更好 / 光看代码说不准"六类归档。
以下只记录**本工具**已落地的修复；**尚未用真实脱敏底稿验证**，所有"已修复"仅代表通过了合成回归。

### 已修复

- **首次读取不落缓存**：`ts.inspect` 原走不落缓存的读取路径，同一文件在一次会话里被完整解析三次（加载、取筛选值、导出）。现首次读取即写稳定 Parquet 缓存。
- **多 Sheet 静默取第一个**：新增醒目提示，说明当前用的是哪张表、共有几张。
- **导出结果不显示**：引擎返回的 by经理/by项目行数、明细行数、耗时现已在界面呈现。
- **候选值截断无提示**：显示"已加载 N / 共 M"。
- **读取阶段无进度、无法取消**：`ts.inspect` 改走任务通道。
- **耗时预期文案**：旧版界面明写"导出需要 5 分钟""勾选会额外等待约 1 分钟"，迁移时删光，现已补回。

### 结果差异（新版更正确，但与旧版不同）

- **旧版会把透视表里空白的行标签填成上一行的值**（李四职级为空即显示成张三的职级），且旧版行序随机，填入的值本身也随机——这是凭空造数。新版留空。**若历史上有基于旧版导出做过按职级/类型维度的分析，结论需要复核。**
- **旧版筛选值填错会静默丢弃整条筛选条件**并导出全量；新版精确匹配，匹配不到即 0 行。
- **旧版缓存文件名用了每次启动都会变的哈希**，跨会话从不命中；新版用内容指纹，真正复用。

### 与看账共用数据层的连带影响

`tabular.rs` 由 TS 与看账共用，以下三处为看账定的口径同时改变了 TS 的行为，任何一处调整都需同时回归 TS：金额解析（去千分位）、整行全空即丢弃、单元格转文本规则（决定项目编码形态与月份列表头）。
